// # Native ELF const-interp entry
//
// Logline: Re-export elf_eval_main. Old name elf_eval_println_add still works.
//
// Setup: Bodies live in elf_stmt.oo so concat does not skip them.
//
// Beats:
//   1. elf_eval_main walks main. println(add) still evaluates.

import "elf_stmt.oo";

pub fn elf_eval_println_add(src: String) -> Int {
    return elf_eval_main(src);
}
