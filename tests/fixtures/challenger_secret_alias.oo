// # Secret Test Alias
// Logline: Alias chain secret sink refusal test.
// Setup: None.
// Beats: 1. Main.
// SECRET: secret_key
pub fn main() {
    let secret_key = "sensitive_value";
    let my_alias = secret_key;
    println(my_alias);
}
