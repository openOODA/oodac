// # Challenger AArch64 Sample
// Logline: Valid AArch64 constrained source for codegen.
// Setup: None.
// Beats: 1. Arithmetic and branch.
fn main() {
    let a: Int = 2;
    let b: Int = 3;
    let c: Int = a + b;
    let d: Int = c - 1;
    if c > 4 {
        return c * 2;
    }
    return d;
}
