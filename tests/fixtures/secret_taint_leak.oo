// # Secret Taint Leak Fixture
//
// Logline: Leaks type-level secret to unsealed sink println.
//
// Setup: Secret taint dataflow tracking must catch propagation to println.
//
// Beats:
//   1. Declare secret token.
//   2. Propagate token through variable assignment.
//   3. Leak token to unsealed sink.

pub fn leak_secret_value() {
    let secret_key: String = "sk-live-0123456789abcdef";
    let leaked_alias = secret_key;
    println(leaked_alias);
}
