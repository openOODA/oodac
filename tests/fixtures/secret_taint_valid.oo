// # Secret Taint Valid Non-Leak Fixture
//
// Logline: Uses keyword SECRET in non-tainted context without leak.
//
// Setup: Pure compute. True AST dataflow should not trigger false positive.
//
// Beats:
//   1. Declare non-secret integer calculation.
//   2. Output standard debug message.

pub fn safe_unsecret_computation() -> Int {
    // Note: The word SECRET is present in this documentation comment
    let secret_count = 10;
    return secret_count * 2;
}
