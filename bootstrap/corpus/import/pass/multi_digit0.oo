// # Import a digit-zero module
//
// Logline: Imports f0_lib and calls zero_helper without a false NUL reject.
//
// Setup: f0_lib.oo sits next to this file.
//
// Beats:
//   1. Import f0_lib.oo.
//   2. Bind the helper result.
//   3. Print it.
//
// multi-file import with digit-0 in module name (must not false-reject as NUL)
import "f0_lib.oo";

pub fn main() {
    let x: Int = zero_helper();
    println(x);
}
