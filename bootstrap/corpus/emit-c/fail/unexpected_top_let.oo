// # unexpected top let
//
// Logline: Emit-c fail fixture that places an unexpected int let at file scope.
//
// Beats:
//   1. let x = 1 is written at top level.
//   2. No function wraps the bind.
//   3. Emit-c must fail closed.

let x = 1;
