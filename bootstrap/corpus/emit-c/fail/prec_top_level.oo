// # prec top level
//
// Logline: Emit-c fail fixture that places a top-level if with a boolean or.
//
// Beats:
//   1. An if compares 1 == 1 || 2 == 2.
//   2. The body is empty.
//   3. Emit-c must reject the top-level item.

if 1 == 1 || 2 == 2 { }
