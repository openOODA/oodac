// # Extra closing brace
//
// Logline: Closes main then emits a stray } so parse fails on balance.
//
// Setup: parse fail fixture for unbalanced braces.
//
// Beats:
//   1. Open main and return 0.
//   2. Close the function.
//   3. Leave one extra }.
//
// parse fail: unbalanced brace — extra top-level } after fn body
pub fn main() {
    return 0;
}
}
