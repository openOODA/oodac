// # ok now ms
//
// Logline: Check-pass fixture that grants now_ms through a TimeCap token.
//
// Beats:
//   1. tick takes t: &TimeCap.
//   2. now_ms is called with that token.
//   3. Check must accept the clock read.

pub fn tick(t: &TimeCap) -> Int {
    return now_ms(t);
}
pub fn main() {}
