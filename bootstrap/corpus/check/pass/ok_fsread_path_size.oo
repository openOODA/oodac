// # ok fsread path size
//
// Logline: Check-pass fixture that grants path_exists and file_size through FsReadCap.
//
// Beats:
//   1. ok takes fs: &FsReadCap.
//   2. path_exists and file_size both receive that token.
//   3. Check must accept the read-side pair.

pub fn ok(fs: &FsReadCap) {
    let a = path_exists(fs, "/tmp/cap_g2_x");
    let b = file_size(fs, "/tmp/cap_g2_x");
}
pub fn main() {}
