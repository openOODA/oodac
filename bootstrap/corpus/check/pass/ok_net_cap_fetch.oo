// # ok net cap fetch
//
// Logline: Check-pass fixture that declares NetCap and calls fetch for static check only.
//
// Beats:
//   1. ok takes net: &NetCap.
//   2. fetch is called with that token.
//   3. Check must accept the declared grant.

pub fn ok(net: &NetCap) {
    let r = fetch(net, "http://example.invalid");
}
pub fn main() {}
