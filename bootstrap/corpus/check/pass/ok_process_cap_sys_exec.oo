// # ok process cap sys exec
//
// Logline: Check-pass fixture that grants sys_exec through a ProcessCap token.
//
// Beats:
//   1. main takes p: &ProcessCap.
//   2. sys_exec is called with that token.
//   3. Check must accept the preferred process grant.

fn main(p: &ProcessCap) {
    let _ = sys_exec(p, "true");
}
