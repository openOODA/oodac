// # ok fs write
//
// Logline: Check-pass fixture that grants write_file through an FsCap token.
//
// Beats:
//   1. ok takes fs: &FsCap.
//   2. write_file is called with that token.
//   3. Check must accept the sealed write.

pub fn ok(fs: &FsCap) {
    let r = write_file(fs, "/tmp/x", "hi");
}
pub fn main() {}
