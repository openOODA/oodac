// # ok fs write cap write
//
// Logline: Check-pass fixture that grants write_file through a precise FsWriteCap token.
//
// Beats:
//   1. ok takes fs: &FsWriteCap.
//   2. write_file is called with that token.
//   3. Check must accept the granular grant.

pub fn ok(fs: &FsWriteCap) {
    let r = write_file(fs, "/tmp/x", "hi");
}
pub fn main() {}
