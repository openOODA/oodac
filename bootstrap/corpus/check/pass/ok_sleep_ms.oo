// # ok sleep ms
//
// Logline: Check-pass fixture that grants sleep_ms through a TimeCap named time.
//
// Beats:
//   1. main takes time: &TimeCap.
//   2. sleep_ms is called with that token.
//   3. Check must accept the named grant inject.

pub fn main(time: &TimeCap) {
    let r = sleep_ms(time, 1);
    println(1);
}
