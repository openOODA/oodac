// # ok seed
//
// Logline: Check-pass fixture that grants seed through a RandCap named rand.
//
// Beats:
//   1. main takes rand: &RandCap.
//   2. seed is called with that token.
//   3. Check must accept the named grant inject.

pub fn main(rand: &RandCap) {
    let r = seed(rand, 1);
    println(1);
}
