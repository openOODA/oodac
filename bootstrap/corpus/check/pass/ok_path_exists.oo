// # ok path exists
//
// Logline: Check-pass fixture that grants path_exists through an FsCap token.
//
// Beats:
//   1. ok takes fs: &FsCap.
//   2. path_exists is called with that token.
//   3. Check must accept the path probe.

pub fn ok(fs: &FsCap) {
    let e = path_exists(fs, "/tmp");
}
pub fn main() {}
