// # ok http cap fetch
//
// Logline: Check-pass fixture that grants fetch through a precise HttpCap token.
//
// Beats:
//   1. ok takes http: &HttpCap.
//   2. fetch is called with that token.
//   3. Check must accept the granular grant.

pub fn ok(http: &HttpCap) {
    let r = fetch(http, "http://example.invalid");
}
pub fn main() {}
