// # ok http cap fetch
//
// Logline: Check-pass fixture that grants fetch through a precise TcpCap token.
//
// Beats:
//   1. ok takes tcp: &TcpCap.
//   2. fetch is called with that token.
//   3. Check must accept the granular grant.

pub fn ok(tcp: &TcpCap) {
    let r = fetch(tcp, "http://example.invalid");
}
pub fn main() {}
