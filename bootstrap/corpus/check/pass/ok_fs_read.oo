// # ok fs read
//
// Logline: Check-pass fixture that grants read_file through an FsCap token.
//
// Beats:
//   1. ok takes fs: &FsCap.
//   2. read_file is called with that token.
//   3. Check must accept the sealed read.

pub fn ok(fs: &FsCap) {
    let r = read_file(fs, "/tmp/x");
}
pub fn main() {}
