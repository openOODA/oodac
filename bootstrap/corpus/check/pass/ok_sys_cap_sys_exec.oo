// # ok sys cap sys exec
//
// Logline: Check-pass fixture that lets legacy SysCap authorize sys_exec.
//
// Beats:
//   1. main takes sys: &SysCap.
//   2. sys_exec is called with that token.
//   3. Check must accept the supersede grant.

fn main(sys: &SysCap) {
    let _ = sys_exec(sys, "true");
}
