// # ok unsafe ffi host
//
// Logline: Check-pass fixture that grants host_ast_dump through an UnsafeFFICap token.
//
// Beats:
//   1. ok takes ffi: &UnsafeFFICap.
//   2. host_ast_dump is called with that token.
//   3. Check must accept the host dump.

pub fn ok(ffi: &UnsafeFFICap) {
    let r = host_ast_dump(ffi, "x.oo");
}
pub fn main() {}
