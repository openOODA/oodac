// # no cap alloc bytes
//
// Logline: Check-fail fixture that calls alloc_bytes with no AllocCap in scope.
//
// Beats:
//   1. grab takes only an Int size.
//   2. alloc_bytes is called on that size.
//   3. Check must deny the sealed alloc.

pub fn grab(n: Int) -> Int {
    return alloc_bytes(n);
}
pub fn main() {}
