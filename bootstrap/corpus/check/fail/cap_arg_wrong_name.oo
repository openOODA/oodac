// # cap arg wrong name
//
// Logline: Check-fail fixture that passes a path IDENT as the first write_file argument instead of the FsCap token.
//
// Beats:
//   1. main takes fs: &FsCap and binds a path string.
//   2. write_file is called with path then payload.
//   3. Check must reject the misnamed first argument.

pub fn main(fs: &FsCap) {
    let path = "/tmp/x";
    let r = write_file(path, "y");
}
