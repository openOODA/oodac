// # wrong granular fswrite for read
//
// Logline: Check-fail fixture that holds FsWriteCap and must not authorize read_file.
//
// Beats:
//   1. bad takes fs: &FsWriteCap.
//   2. read_file is called with that token.
//   3. Check must keep the grant precise.

pub fn bad(fs: &FsWriteCap) {
    let r = read_file(fs, "/tmp/x");
}
pub fn main() {}
