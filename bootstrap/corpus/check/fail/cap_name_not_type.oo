// # cap name not type
//
// Logline: Check-fail fixture that names a param FsCap but types it Int, which must not grant sealed reads.
//
// Beats:
//   1. main takes FsCap: Int.
//   2. read_file is called with no real capability.
//   3. Check must fail closed.

pub fn main(FsCap: Int) {
    let r = read_file("README.md");
    println("should not check OK");
}
