// # no cap env get
//
// Logline: Check-fail fixture that calls env_get with no EnvCap in scope.
//
// Beats:
//   1. rogue takes no capability params.
//   2. env_get is called on HOME.
//   3. Check must default-deny the env read.

pub fn rogue() {
    let r = env_get("HOME");
}
pub fn main() {}
