// # no cap ffi call
//
// Logline: Check-fail fixture that calls ffi_call without an FfiCap token.
//
// Beats:
//   1. rogue takes no capability params.
//   2. ffi_call is invoked with a symbol token and specs.
//   3. Check must deny the FFI call.

pub fn rogue() {
    let r = ffi_call("sym:0x0", "i", "", "");
}
pub fn main() {}
