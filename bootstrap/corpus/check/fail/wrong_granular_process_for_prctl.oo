// # wrong granular process for prctl
//
// Logline: Check-fail fixture that holds ProcessCap and must not unlock sys_prctl.
//
// Beats:
//   1. main takes p: &ProcessCap.
//   2. sys_prctl is called with that token.
//   3. Check must keep the hard SysCap residual sealed.

fn main(p: &ProcessCap) {
    let _ = sys_prctl(p, 0);
}
