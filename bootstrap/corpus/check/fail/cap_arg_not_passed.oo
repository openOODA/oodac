// # cap arg not passed
//
// Logline: Check-fail fixture where &FsCap is in scope but write_file is called without that token.
//
// Beats:
//   1. main takes fs: &FsCap.
//   2. write_file is called with only path and payload.
//   3. Check must reject the sealed call.

pub fn main(fs: &FsCap) {
    let r = write_file("/tmp/x", "y");
}
