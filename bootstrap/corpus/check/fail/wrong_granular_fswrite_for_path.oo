// # wrong granular fswrite for path
//
// Logline: Check-fail fixture that holds FsWriteCap and must not authorize path_exists.
//
// Beats:
//   1. bad takes fs: &FsWriteCap.
//   2. path_exists is called with that token.
//   3. Check must keep the read-side grant off the write cap.

pub fn bad(fs: &FsWriteCap) {
    let a = path_exists(fs, "/tmp/cap_g2_x");
}
pub fn main() {}
