// # no cap host ast dump
//
// Logline: Check-fail fixture that calls host_ast_dump without an UnsafeFFICap token.
//
// Beats:
//   1. rogue takes no capability params.
//   2. host_ast_dump is called on a source path.
//   3. Check must seal the host dump.

pub fn rogue() {
    let r = host_ast_dump("x.oo");
}
pub fn main() {}
