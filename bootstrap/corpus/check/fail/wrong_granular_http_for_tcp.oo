// # wrong granular http for tcp
//
// Logline: Check-fail fixture that holds TcpCap and must not authorize tcp_bind.
//
// Beats:
//   1. bad takes tcp: &TcpCap.
//   2. tcp_bind is called with that token.
//   3. Check must keep BindCap precise.

pub fn bad(tcp: &TcpCap) {
    let r = tcp_bind(tcp, 8080);
}
pub fn main() {}
