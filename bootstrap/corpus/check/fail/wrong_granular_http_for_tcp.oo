// # wrong granular http for tcp
//
// Logline: Check-fail fixture that holds HttpCap and must not authorize tcp_bind.
//
// Beats:
//   1. bad takes http: &HttpCap.
//   2. tcp_bind is called with that token.
//   3. Check must keep the net grant precise.

pub fn bad(http: &HttpCap) {
    let r = tcp_bind(http, 8080);
}
pub fn main() {}
