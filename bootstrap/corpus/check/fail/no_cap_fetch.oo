// # no cap fetch
//
// Logline: Check-fail fixture that calls fetch with no net capability in scope.
//
// Beats:
//   1. rogue takes no capability params.
//   2. fetch is called on an http URL.
//   3. Check must default-deny the request.

pub fn rogue() {
    let r = fetch("http://evil.example");
}
pub fn main() {}
