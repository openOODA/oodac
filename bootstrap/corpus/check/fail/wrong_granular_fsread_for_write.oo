// # wrong granular fsread for write
//
// Logline: Check-fail fixture that holds FsReadCap and must not authorize write_file.
//
// Beats:
//   1. bad takes fs: &FsReadCap.
//   2. write_file is called with that token.
//   3. Check must keep the grant precise.

pub fn bad(fs: &FsReadCap) {
    let r = write_file(fs, "/tmp/x", "hi");
}
pub fn main() {}
