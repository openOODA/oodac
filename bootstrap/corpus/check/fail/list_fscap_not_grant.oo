// # list fscap not grant
//
// Logline: Check-fail fixture that holds List[FsCap] and must not unlock a free read_file.
//
// Beats:
//   1. main takes xs: List[FsCap].
//   2. read_file is called with only a path.
//   3. Check must treat the list as a non-grant.

pub fn main(xs: List[FsCap]) {
    let r = read_file("README.md");
    println("should not check OK");
}
