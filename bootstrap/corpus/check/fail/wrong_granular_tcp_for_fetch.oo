// # wrong granular tcp for fetch
//
// Logline: Check-fail fixture that holds TcpCap and must not authorize fetch.
//
// Beats:
//   1. bad takes tcp: &TcpCap.
//   2. fetch is called with that token.
//   3. Check must keep the net grant precise.

pub fn bad(tcp: &TcpCap) {
    let r = fetch(tcp, "http://evil.example");
}
pub fn main() {}
