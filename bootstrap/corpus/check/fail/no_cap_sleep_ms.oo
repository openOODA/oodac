// # no cap sleep ms
//
// Logline: Check-fail fixture that calls sleep_ms with no TimeCap in scope.
//
// Beats:
//   1. main takes no capability params.
//   2. sleep_ms is called with a literal 1.
//   3. Check must deny the sleep.

pub fn main() {
    let r = sleep_ms(1);
    println(1);
}
