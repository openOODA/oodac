// # no cap now ms
//
// Logline: Check-fail fixture that calls now_ms with no TimeCap in scope.
//
// Beats:
//   1. tick takes no capability params.
//   2. now_ms is returned directly.
//   3. Check must deny the clock read.

pub fn tick() -> Int {
    return now_ms();
}
pub fn main() {}
