// # no cap sys exec
//
// Logline: Check-fail fixture that calls sys_exec with no SysCap in scope.
//
// Beats:
//   1. rogue takes no capability params.
//   2. sys_exec is called on true.
//   3. Check must default-deny the exec.

pub fn rogue() {
    let r = sys_exec("true");
}
pub fn main() {}
