// # no cap chs build
//
// Logline: Check-fail fixture that calls chs_build without an UnsafeFFICap token.
//
// Beats:
//   1. rogue takes no capability params.
//   2. chs_build is called on two string paths.
//   3. Check must seal the host build.

pub fn rogue() {
    let r = chs_build("a.oo", "out.bin");
}
pub fn main() {}
