// # no cap file size
//
// Logline: Check-fail fixture that calls file_size with no FsCap in scope.
//
// Beats:
//   1. main takes no capability params.
//   2. file_size is called on README.md.
//   3. Check must default-deny the size query.

pub fn main() {
    let n = file_size("README.md");
    println(n);
}
