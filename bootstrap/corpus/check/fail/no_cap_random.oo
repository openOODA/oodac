// # no cap random
//
// Logline: Check-fail fixture that calls random with no RandCap in scope.
//
// Beats:
//   1. roll takes no capability params.
//   2. random is returned directly.
//   3. Check must deny the entropy call.

pub fn roll() -> Int {
    return random();
}
pub fn main() {}
