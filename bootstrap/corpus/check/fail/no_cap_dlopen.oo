// # no cap dlopen
//
// Logline: Check-fail fixture that calls dlopen without an UnsafeFFICap token.
//
// Beats:
//   1. rogue takes no capability params.
//   2. dlopen is called on a shared-library path.
//   3. Check must deny the FFI open.

pub fn rogue() {
    let r = dlopen("libevil.so");
}
pub fn main() {}
