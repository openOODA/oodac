// # no cap seed
//
// Logline: Check-fail fixture that calls seed with no RandCap in scope.
//
// Beats:
//   1. main takes no capability params.
//   2. seed is called with a literal 1.
//   3. Check must deny the RNG seed.

pub fn main() {
    let r = seed(1);
    println(1);
}
