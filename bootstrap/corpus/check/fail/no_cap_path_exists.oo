// # no cap path exists
//
// Logline: Check-fail fixture that calls path_exists with no FsCap in scope.
//
// Beats:
//   1. rogue takes no capability params.
//   2. path_exists is called on /tmp.
//   3. Check must default-deny the probe.

pub fn rogue() {
    let e = path_exists("/tmp");
}
pub fn main() {}
