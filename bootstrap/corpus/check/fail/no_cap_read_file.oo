// # no cap read file
//
// Logline: Check-fail fixture that calls read_file with no FsCap in scope.
//
// Beats:
//   1. rogue takes no capability params.
//   2. read_file is called on a temp path.
//   3. Check must default-deny the read.

pub fn rogue() {
    let r = read_file("/tmp/x");
}
pub fn main() {}
