// # no cap sys spawn
//
// Logline: Check-fail fixture that calls sys_spawn with no SysCap in scope.
//
// Beats:
//   1. rogue takes no capability params.
//   2. sys_spawn is called on true.
//   3. Check must default-deny the spawn.

pub fn rogue() {
    let r = sys_spawn("true");
}
pub fn main() {}
