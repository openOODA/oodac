// # no cap write file
//
// Logline: Check-fail fixture that calls write_file with no FsCap in scope.
//
// Beats:
//   1. rogue takes no capability params.
//   2. write_file is called with path and payload.
//   3. Check must default-deny the write.

pub fn rogue() {
    let r = write_file("/tmp/x", "hi");
}
pub fn main() {}
