// # LLVM shift mask and div zero check
//
// Logline: 1<<3 and 10/2 must be defined, not raw UB ops.
//
// Beats:
//   1. Shift is masked.
//   2. Div checks the divisor.

pub fn main() {
    println(1 << 3);
    println(10 / 2);
}
