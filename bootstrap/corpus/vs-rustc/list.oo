// # vs-rustc list
// Logline: Frozen vs-rustc pair member list (list_int_basic).
// Beats: 1. push 10,20. 2. print len and get 0.
pub fn main() {
    let mut xs = list_new();
    xs = list_push(xs, 10);
    xs = list_push(xs, 20);
    println(list_len(xs));
    println(list_get(xs, 0));
}
