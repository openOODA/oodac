// # Accept Int var plus
//
// Logline: Adding two Int locals must typecheck.
//
// Setup: a and b are inferred Int.
//
// Beats:
//   1. Bind a and b.
//   2. Add them.
//   3. Typecheck must accept the binop.

pub fn main() {
    let a = 1;
    let b = 2;
    let c = a + b;
    println(c);
}
