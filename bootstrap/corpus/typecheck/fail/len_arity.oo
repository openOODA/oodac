// # Len with an extra argument
//
// Logline: Calls len with one argument when it takes none.
//
// Setup: oodac typecheck must reject this program.
//
// Beats:
//   1. Bind x to "hi".
//   2. Call x.len(1).
//   3. Fail arity.

pub fn main() {
    let x = "hi";
    println(x.len(1));
}
