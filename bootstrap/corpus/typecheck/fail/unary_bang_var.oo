// # Reject bang on Int var
//
// Logline: Logical not on an Int variable must fail typecheck.
//
// Setup: Bang requires Bool; x is inferred Int.
//
// Beats:
//   1. Bind x to 1.
//   2. Write !x.
//   3. Typecheck must reject the operand.

pub fn main() {
    let x = 1;
    let y = !x;
}
