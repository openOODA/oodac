// # And an int with true
//
// Logline: Uses && between 1 and true.
//
// Setup: oodac typecheck must reject this program.
//
// Beats:
//   1. Open main.
//   2. Bind x to 1 && true.
//   3. Fail the logic types.

pub fn main() {
    let x = 1 && true;
}
