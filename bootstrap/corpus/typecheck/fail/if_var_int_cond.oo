// # If on an int var
//
// Logline: Uses an Int local as an if condition.
//
// Setup: oodac typecheck must reject this program.
//
// Beats:
//   1. Bind x to 1.
//   2. Test if x.
//   3. Fail the non-bool condition.

pub fn main() {
    let x = 1;
    if x {
        println(1);
    }
}
