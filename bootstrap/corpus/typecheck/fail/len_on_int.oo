// # Len on an int
//
// Logline: Calls len on an Int local.
//
// Setup: oodac typecheck must reject this program.
//
// Beats:
//   1. Bind x to 1.
//   2. Call x.len().
//   3. Fail the method on Int.

pub fn main() {
    let x = 1;
    println(x.len());
}
