// # Pass a field as a string
//
// Logline: Passes Point.x into a String parameter.
//
// Setup: oodac typecheck must reject this program.
//
// Beats:
//   1. Define Point and f taking String.
//   2. Bind p.
//   3. Call f(p.x).

type Point = struct {
    x: Int,
    y: Int,
};
pub fn f(a: String) {}
pub fn main() {
    let p = Point { x: 1, y: 2 };
    f(p.x);
}
