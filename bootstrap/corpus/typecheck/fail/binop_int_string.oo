// # Add int to string
//
// Logline: Adds 1 + "hi" so mixed literal arithmetic fails typecheck.
//
// Setup: oodac typecheck must reject this program (parity with stage-0).
//
// Beats:
//   1. Open main.
//   2. Bind x to 1 + "hi".
//   3. Fail the mixed binop.
//
// Pure lit Int + String must fail oodac typecheck slice (parity with stage-0).
pub fn main() {
    let x = 1 + "hi";
}
