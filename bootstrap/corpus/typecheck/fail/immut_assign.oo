// # Assign an immutable local
//
// Logline: Writes a second value into a non-mut let.
//
// Setup: oodac typecheck must reject this program.
//
// Beats:
//   1. Bind x to 1 without mut.
//   2. Assign x = 2.
//   3. Fail the immutable store.

pub fn main() {
    let x = 1;
    x = 2;
}
