// # Reject shift left
//
// Logline: Integer shift-left is not a product typecheck operator.
//
// Setup: R1 typecheck has no `<<` binop.
//
// Beats:
//   1. Write `1 << 2`.
//   2. Typecheck walks the binop.
//   3. The operator must fail closed.

pub fn main() {
    let x = 1 << 2;
}
