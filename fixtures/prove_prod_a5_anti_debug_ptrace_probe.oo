// # Product prove std/sec/defense/anti_debug_ptrace_probe.oo
//
// Logline: Fail-closed emit+gcc+run asserts tracer_status_from_pid is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/defense/anti_debug_ptrace_probe.oo.
//
// Beats:
//   1. Call tracer_status_from_pid twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_anti_debug_ptrace_probe: PASSED only if they match.

import "std/sec/defense/anti_debug_ptrace_probe.oo";

pub fn main() {
    let anti_debug_ptrace_probe_mark: Int = 0;
    if anti_debug_ptrace_probe_mark != 0 {
        process_exit(1);
    }
    let a: TracerStatus = tracer_status_from_pid(0);
    let b: TracerStatus = tracer_status_from_pid(0);
    if (a).tracer_pid != (b).tracer_pid {
        println("FAIL tracer_status_from_pid");
        process_exit(1);
    }
    println("prod_anti_debug_ptrace_probe: PASSED");
}
