// # Product prove std/econ/fin/orderbook_ops.oo
//
// Logline: Fail-closed emit+gcc+run asserts limit_order_remaining_qty is deterministic.
//
// Setup: Isolated emit-c. Imports std/econ/fin/orderbook_ops.oo.
//
// Beats:
//   1. Call limit_order_remaining_qty twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_orderbook_remaining: PASSED only if they match.

import "std/science/econ/fin/orderbook_ops.oo";

pub fn main() {
    let orderbook_ops_mark: Int = 0;
    if orderbook_ops_mark != 0 { process_exit(1); }
    let ord: LimitOrder = LimitOrder { order_id: 1, price: 100, quantity: 50, filled: 20, is_buy: 0, is_canceled: 0 };
    let a: Int = limit_order_remaining_qty(ord);
    let b: Int = limit_order_remaining_qty(ord);
    if a != b {
        println("FAIL limit_order_remaining_qty");
        process_exit(1);
    }
    println("prod_orderbook_remaining: PASSED");
    process_exit(0);
}
