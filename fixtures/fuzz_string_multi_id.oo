// # String arity-2 fuzz pass
//
// Logline: Return the first of two strings so the arity-2 string rail can pass.
//
// Beats:
//   1. Keep FUZZ_DOMAIN string and target pick 0 6.
//   2. Ensure result == x and return x.
//   3. Leave main empty; the fuzzer drives the target.

// M106 pure String arity-2 multi-arg fuzz pass
// FUZZ_DOMAIN: string
// FUZZ_TARGET: pick 0 6
// FUZZ_ENSURES: pick result == x
fn pick(x: String, y: String) -> String {
    return x;
}

pub fn main() {
}
