// # Product prove std/ai/deep/deep_ai_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts deep_ai_stubs_marker is deterministic.
//
// Setup: Isolated emit-c. Imports std/ai/deep/deep_ai_stubs.oo.
//
// Beats:
//   1. Call deep_ai_stubs_marker twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_deep_ai_stubs: PASSED only if they match.

import "std/app/ai/deep/deep_ai_stubs.oo";

pub fn main() {
    let a: Int = deep_ai_stubs_marker();
    let b: Int = deep_ai_stubs_marker();
    if a != b {
        println("FAIL deep_ai_stubs_marker");
        process_exit(1);
    }
    println("prod_deep_ai_stubs: PASSED");
}
