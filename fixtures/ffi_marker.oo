// # FFI residual marker
//
// Logline: Name the FFI residual and note that bare dlopen needs UnsafeFFICap.
//
// Beats:
//   1. Keep the FFI residual comment.
//   2. Note path A needs &UnsafeFFICap at check.
//   3. Print 0 from main.

// Cap vs FFI: marker + path A note (see bootstrap/CAP_FFI.oot)
// FFI: residual (full C TCB / raw-pointer / compile-time FFI gen still residual)
// Path A In: bare dlopen/host-FFI free names need &UnsafeFFICap at check.
pub fn main() {
    println(0);
}
