// # Name the playground residual
//
// Logline: This marker names the playground residual surface without enforcing it.
//
// Beats:
//   1. Keep the residual marker line for honesty smokes.
//   2. Point at bootstrap/PLAYGROUND.oot as the surface doc.
//   3. Print zero so a named-only fixture still parses.

// Residual marker — see bootstrap/PLAYGROUND.oot
// PLAYGROUND: residual
pub fn main() { println(0); }
