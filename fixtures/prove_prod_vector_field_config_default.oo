// # Product prove std/robot/auv/auv_vector_field_guidance.oo
//
// Logline: Fail-closed emit+gcc+run asserts vector_field_config_default is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/auv/auv_vector_field_guidance.oo.
//
// Beats:
//   1. Call vector_field_config_default twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_vector_field_config_default: PASSED only if they match.

import "std/app/robot/auv/auv_vector_field_guidance.oo";

pub fn main() {
    let auv_vector_field_guidance_mark: Int = 0;
    if auv_vector_field_guidance_mark != 0 { process_exit(1); }
    let a: VectorFieldConfig = vector_field_config_default();
    let b: VectorFieldConfig = vector_field_config_default();
    if (a).k_convergence != (b).k_convergence {
        println("FAIL vector_field_config_default");
        process_exit(1);
    }
    println("prod_vector_field_config_default: PASSED");
}
