// # Product prove std/robot/predictive/ts_isolation_forest_score.oo
//
// Logline: Fail-closed emit+gcc+run asserts int_to_float is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/predictive/ts_isolation_forest_score.oo.
//
// Beats:
//   1. Call int_to_float twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ts_isolation_forest_score: PASSED only if they match.

import "std/app/robot/predictive/ts_isolation_forest_score.oo";

pub fn main() {
    let ts_isolation_forest_score_mark: Int = 0;
    if ts_isolation_forest_score_mark != 0 {
        process_exit(1);
    }
    let a: Float = int_to_float(0);
    let b: Float = int_to_float(0);
    if a != b {
        println("FAIL int_to_float");
        process_exit(1);
    }
    println("prod_ts_isolation_forest_score: PASSED");
}
