// # Product prove std/ui/web_ecosystem/lodash_flatten.oo
//
// Logline: Fail-closed emit+gcc+run asserts lodash_flatten_n is deterministic.
//
// Setup: Isolated emit-c. Imports std/ui/web_ecosystem/lodash_flatten.oo.
//
// Beats:
//   1. Call lodash_flatten_n twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_lodash_flatten: PASSED only if they match.

import "std/app/ui/web_ecosystem/lodash_flatten.oo";

pub fn main() {
    let a: Int = lodash_flatten_n(0, 0);
    let b: Int = lodash_flatten_n(0, 0);
    if a != b {
        println("FAIL lodash_flatten_n");
        process_exit(1);
    }
    println("prod_lodash_flatten: PASSED");
}
