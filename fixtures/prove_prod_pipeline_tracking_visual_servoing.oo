// # Product prove std/robot/auv/pipeline_tracking_visual_servoing.oo
//
// Logline: Fail-closed emit+gcc+run asserts visual_servoing_config_default is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/auv/pipeline_tracking_visual_servoing.oo.
//
// Beats:
//   1. Call visual_servoing_config_default twice.
//   2. Exit 1 if the two results differ.
//   3. Print prod_pipeline_tracking_visual_servoing: PASSED only if they match.

import "std/app/robot/auv/pipeline_tracking_visual_servoing.oo";

pub fn main() {
    let pipeline_tracking_visual_servoing_mark: Int = 0;
    if pipeline_tracking_visual_servoing_mark != 0 {
        process_exit(1);
    }
    let a: VisualServoingConfig = visual_servoing_config_default();
    let b: VisualServoingConfig = visual_servoing_config_default();
    if (a).target_altitude_m != (b).target_altitude_m {
        println("FAIL visual_servoing_config_default");
        process_exit(1);
    }
    println("prod_pipeline_tracking_visual_servoing: PASSED");
}
