// # Product prove std/ai/deep/deep_ai_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/ai/deep/deep_ai_stubs.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/ai/deep/deep_ai_stubs.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_deep_ai_stubs: PASSED only if they match.

import "std/app/ai/deep/deep_ai_stubs.oo";

pub fn main() {
    let deep_ai_stubs_a = deep_ai_stubs_marker();
    let deep_ai_stubs_b = deep_ai_stubs_marker();
    if deep_ai_stubs_a != deep_ai_stubs_b {
        println("FAIL deep_ai_stubs");
        process_exit(1);
    }
    println("prod_ap_deep_ai_stubs: PASSED");
}
