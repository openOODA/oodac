// # Import User Fixture
// Logline: Imports Point and calls methods on imported struct.
// Setup: Imports fixtures/m5/point_mod.oo.
// Beats: Calls point_scale and point_sum on Point instance.

import "fixtures/m5/point_mod.oo";

pub fn main() {
    let p: Point = Point { x: 5, y: 7 };
    let p2: Point = p.point_scale(4);
    let s: Int = p2.point_sum();
    println("import_sum = ", s);
}
