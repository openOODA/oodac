// # Auto-generated signature-aware smoke stub for std/compile/llvm_codegen/llvm_verify_strings.oo.
//
// Logline: Exercises pure string functions and verify blocks on the LLVM backend.
// Setup: Pure string helper functions with verify blocks and main execution assertions.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_LLVM_CODEGEN_LLVM_VERIFY_STRINGS_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/compile/llvm_codegen/llvm_verify_strings.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: String = join_slug("x", "x", "x");
    let r2: String = join_slug("x", "x", "x");
    if !expect_eq_int("r len", r.len(), r.len()) { return; }
    if !expect_eq_int("r2 len", r2.len(), r2.len()) { return; }
    println("STD_LLVM_CODEGEN_LLVM_VERIFY_STRINGS_OO_STUB_OK");
}
