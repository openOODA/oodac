// # Name the meta-vs-det residual
//
// Logline: This marker names the meta-versus-deterministic residual without enforcing it.
//
// Beats:
//   1. Keep the residual marker line for honesty smokes.
//   2. Point at bootstrap/META_VS_DET.oot as the surface doc.
//   3. Print zero so a named-only fixture still parses.

// Residual marker fixture — see bootstrap/META_VS_DET.oot
// META_VS_DET: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
