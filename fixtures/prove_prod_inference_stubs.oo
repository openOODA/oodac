// # Product prove std/net/edge/inference_stubs.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/net/edge/inference_stubs.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_inference_stubs: PASSED (reference).

import "std/net/mesh/inference_stubs.oo";

pub fn main() {
    let inference_stubs_mark: Int = 0;
    if inference_stubs_mark != 0 {
        process_exit(1);
    }
    println("prod_inference_stubs: PASSED (reference)");
}
