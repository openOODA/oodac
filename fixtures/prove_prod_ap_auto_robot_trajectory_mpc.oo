// # Product prove std/robot/auto_robot/trajectory_mpc.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/robot/auto_robot/trajectory_mpc.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/auto_robot/trajectory_mpc.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_trajectory_mpc: PASSED only if they match.

import "std/app/robot/auto_robot/trajectory_mpc.oo";

pub fn main() {
    let trajectory_mpc_a = mpc_cte_sq(0, 0);
    let trajectory_mpc_b = mpc_cte_sq(0, 0);
    if trajectory_mpc_a != trajectory_mpc_b {
        println("FAIL trajectory_mpc");
        process_exit(1);
    }
    println("prod_ap_trajectory_mpc: PASSED");
}
