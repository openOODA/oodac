// # Auto-generated signature-aware smoke stub for std/collections/advanced/bloom_filter.oo.
//
// Logline: Tests set membership with a MurmurHash3 bit array and never reports an inserted key as absent.
// Setup: Pure. Keys are Int. Bits pack in 32-bit words as List[Int].
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_COLLECTIONS_ADVANCED_BLOOM_FILTER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/core/collection/advanced/bloom_filter.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = bloom_filter_murmur3(0, 0);
    let r2: Int = bloom_filter_murmur3(0, 0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_COLLECTIONS_ADVANCED_BLOOM_FILTER_OO_STUB_OK");
}
