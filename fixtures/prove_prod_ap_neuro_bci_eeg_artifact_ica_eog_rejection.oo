// # Product prove std/bio/neuro_bci/eeg_artifact_ica_eog_rejection.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/bio/neuro_bci/eeg_artifact_ica_eog_rejection.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/bio/neuro_bci/eeg_artifact_ica_eog_rejection.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_eeg_artifact_ica_eog_rejection: PASSED only if they match.

import "std/science/bio/neuro_bci/eeg_artifact_ica_eog_rejection.oo";

pub fn main() {
    let eeg_artifact_ica_eog_rejection_a = ica_compute_kurtosis_4pt(0.0, 0.0, 0.0, 0.0);
    let eeg_artifact_ica_eog_rejection_b = ica_compute_kurtosis_4pt(0.0, 0.0, 0.0, 0.0);
    if eeg_artifact_ica_eog_rejection_a != eeg_artifact_ica_eog_rejection_b {
        println("FAIL eeg_artifact_ica_eog_rejection");
        process_exit(1);
    }
    println("prod_ap_eeg_artifact_ica_eog_rejection: PASSED");
}
