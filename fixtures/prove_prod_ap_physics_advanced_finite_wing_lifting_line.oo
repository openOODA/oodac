// # Product prove std/phys/advanced/finite_wing_lifting_line.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/advanced/finite_wing_lifting_line.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/advanced/finite_wing_lifting_line.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_finite_wing_lifting_line: PASSED only if they match.

import "std/science/phys/classical/finite_wing_lifting_line.oo";

pub fn main() {
    let finite_wing_lifting_line_a = finite_wing_induced_drag_cdi(0.0, 0.0, 0.0);
    let finite_wing_lifting_line_b = finite_wing_induced_drag_cdi(0.0, 0.0, 0.0);
    if finite_wing_lifting_line_a != finite_wing_lifting_line_b {
        println("FAIL finite_wing_lifting_line");
        process_exit(1);
    }
    println("prod_ap_finite_wing_lifting_line: PASSED");
}
