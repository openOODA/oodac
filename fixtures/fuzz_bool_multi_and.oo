// # Bool arity-2 fuzz pass
//
// Logline: Return the first of two Bools so the arity-2 bool fuzz rail can pass.
//
// Beats:
//   1. Keep FUZZ_DOMAIN bool and target both.
//   2. Ensure result == x and return x.
//   3. Leave main empty; the fuzzer drives the target.

// M56 pure Bool arity-2 multi-arg fuzz pass
// FUZZ_DOMAIN: bool
// FUZZ_TARGET: both
// FUZZ_ENSURES: both result == x
fn both(x: Bool, y: Bool) -> Bool {
    return x;
}

pub fn main() {
}
