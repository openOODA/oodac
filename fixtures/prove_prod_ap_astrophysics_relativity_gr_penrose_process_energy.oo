// # Product prove std/phys/astrophysics_relativity/gr_penrose_process_energy.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/astrophysics_relativity/gr_penrose_process_energy.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/astrophysics_relativity/gr_penrose_process_energy.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_gr_penrose_process_energy: PASSED only if they match.

import "std/science/phys/relativistic/gr_penrose_process_energy.oo";

pub fn main() {
    let gr_penrose_process_energy_a = kerr_irreducible_mass(0, 0);
    let gr_penrose_process_energy_b = kerr_irreducible_mass(0, 0);
    if gr_penrose_process_energy_a != gr_penrose_process_energy_b {
        println("FAIL gr_penrose_process_energy");
        process_exit(1);
    }
    println("prod_ap_gr_penrose_process_energy: PASSED");
}
