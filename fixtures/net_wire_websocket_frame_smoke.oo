// # Auto-generated signature-aware smoke stub for std/net/wire/websocket_frame.oo.
//
// Logline: Builds and parses WebSocket frames with 4-byte client XOR masking and 16-bit or 64-bit extended payload length.
// Setup: Pure. Bytes are List[Int] 0..255. Opcode 1 is text. Opcode 2 is binary.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_NET_WIRE_WEBSOCKET_FRAME_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/net/protocol/websocket_frame.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = websocket_frame_op_text();
    let r2: Int = websocket_frame_op_text();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_NET_WIRE_WEBSOCKET_FRAME_OO_STUB_OK");
}
