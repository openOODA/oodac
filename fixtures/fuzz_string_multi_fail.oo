// # String arity-2 fuzz fail
//
// Logline: Return y while ensuring result == x so the string multi rail fails.
//
// Beats:
//   1. Keep FUZZ_DOMAIN string and target wrong 0 6.
//   2. Ensure result == x but return y.
//   3. Leave main empty; the fuzzer drives the target.

// M106 pure String arity-2 multi-arg fail-rail (returns y, ensures x)
// FUZZ_DOMAIN: string
// FUZZ_TARGET: wrong 0 6
// FUZZ_ENSURES: wrong result == x
fn wrong(x: String, y: String) -> String {
    return y;
}

pub fn main() {
}
