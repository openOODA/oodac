// # Auto-generated signature-aware smoke stub for std/ui/game_engine/ecs_system.oo.
//
// Logline: Organizes phase-ordered system pipelines, execution scheduling, and foundational kinematic dispatch routines.
// Setup: Pipeline phases and system wrappers importing ecs_component.oo, ecs_entity.oo, and ecs_query.oo.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_GAME_ENGINE_ECS_SYSTEM_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/game_engine/ecs_system.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = system_phase_startup();
    let r2: Int = system_phase_startup();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_GAME_ENGINE_ECS_SYSTEM_OO_STUB_OK");
}
