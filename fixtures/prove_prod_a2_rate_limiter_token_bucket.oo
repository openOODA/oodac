// # Product prove std/data/cloud/rate_limiter_token_bucket.oo
//
// Logline: Fail-closed emit+gcc+run asserts token_bucket_new is deterministic.
//
// Setup: Isolated emit-c. Imports std/data/cloud/rate_limiter_token_bucket.oo.
//
// Beats:
//   1. Call token_bucket_new twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_rate_limiter_token_bucket: PASSED only if they match.

import "std/app/data/cloud/rate_limiter_token_bucket.oo";

pub fn main() {
    let rate_limiter_token_bucket_mark: Int = 0;
    if rate_limiter_token_bucket_mark != 0 {
        process_exit(1);
    }
    let a: TokenBucket = token_bucket_new(0, 0, 0);
    let b: TokenBucket = token_bucket_new(0, 0, 0);
    if (a).capacity != (b).capacity {
        println("FAIL token_bucket_new");
        process_exit(1);
    }
    println("prod_rate_limiter_token_bucket: PASSED");
}
