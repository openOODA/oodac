// # Product prove std/ai/multimodal/diffusion_ddpm_scheduler.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/ai/multimodal/diffusion_ddpm_scheduler.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/ai/multimodal/diffusion_ddpm_scheduler.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_diffusion_ddpm_scheduler: PASSED only if they match.

import "std/app/ai/multimodal/diffusion_ddpm_scheduler.oo";

pub fn main() {
    let diffusion_ddpm_scheduler_a = ddpm_beta_at(0, 0, 0, 0);
    let diffusion_ddpm_scheduler_b = ddpm_beta_at(0, 0, 0, 0);
    if diffusion_ddpm_scheduler_a != diffusion_ddpm_scheduler_b {
        println("FAIL diffusion_ddpm_scheduler");
        process_exit(1);
    }
    println("prod_ap_diffusion_ddpm_scheduler: PASSED");
}
