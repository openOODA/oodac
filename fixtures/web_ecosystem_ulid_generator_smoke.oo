// # Auto-generated signature-aware smoke stub for std/ui/web_ecosystem/ulid_generator.oo.
//
// Logline: Encodes 48-bit timestamp and 80-bit randomness into Crockford Base32 26-character sortable identifiers.
// Setup: Pure ULID specification generator with monotonic lexical sorting.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_WEB_ECOSYSTEM_ULID_GENERATOR_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/web_ecosystem/ulid_generator.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: String = crockford_base32_alphabet();
    let r2: String = crockford_base32_alphabet();
    if !expect_eq_int("r len", r.len(), r.len()) { return; }
    if !expect_eq_int("r2 len", r2.len(), r2.len()) { return; }
    println("STD_WEB_ECOSYSTEM_ULID_GENERATOR_OO_STUB_OK");
}
