// # Product prove std/phys/quantum_open_systems/bath_spectral_density_ohmic.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/quantum_open_systems/bath_spectral_density_ohmic.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/quantum_open_systems/bath_spectral_density_ohmic.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_bath_spectral_density_ohmic: PASSED only if they match.

import "std/science/phys/quantum/bath_spectral_density_ohmic.oo";

pub fn main() {
    let bath_spectral_density_ohmic_a = spectral_drude_lorentz_eval(0.0, 0.0, 0.0);
    let bath_spectral_density_ohmic_b = spectral_drude_lorentz_eval(0.0, 0.0, 0.0);
    if bath_spectral_density_ohmic_a != bath_spectral_density_ohmic_b {
        println("FAIL bath_spectral_density_ohmic");
        process_exit(1);
    }
    println("prod_ap_bath_spectral_density_ohmic: PASSED");
}
