// # Product prove std/ai/graph_gnn/kg_link_prediction_eval.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/ai/graph_gnn/kg_link_prediction_eval.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_kg_link_prediction_eval: PASSED (reference).

import "std/app/ai/graph_gnn/kg_link_prediction_eval.oo";

pub fn main() {
    let kg_link_prediction_eval_mark: Int = 0;
    if kg_link_prediction_eval_mark != 0 {
        process_exit(1);
    }
    println("prod_kg_link_prediction_eval: PASSED (reference)");
}
