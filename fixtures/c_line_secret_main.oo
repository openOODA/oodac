// # C Line Secret Main
//
// Logline: Entry point for secret multi-file #line testing.
//
// Setup: Imports fixtures/c_line_secret_lib.oo.
//
// Beats:
//   1. Call get_secret from library.

import "fixtures/c_line_secret_lib.oo";

pub fn main() {
    let safe_val: Int = 100;
    let secret_val: Int = get_secret();
    if secret_val > 0 {
        println(safe_val);
    }
}
