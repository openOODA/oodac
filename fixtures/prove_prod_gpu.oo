// # Product prove std/os/gpu.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/os/gpu.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_gpu: PASSED (reference).

import "std/fs/os/gpu.oo";

pub fn main() {
    let gpu_mark: Int = 0;
    if gpu_mark != 0 {
        process_exit(1);
    }
    println("prod_gpu: PASSED (reference)");
}
