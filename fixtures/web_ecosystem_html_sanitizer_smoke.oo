// # Auto-generated signature-aware smoke stub for std/ui/web_ecosystem/html_sanitizer.oo.
//
// Logline: script/iframe/object/embed/style/form/input/button/meta are dangerous.
// Setup: Tag codes: 1 script 2 iframe 3 object 4 embed 5 style 6 form 7 input 8 button 9 meta.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_WEB_ECOSYSTEM_HTML_SANITIZER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/web_ecosystem/html_sanitizer.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = html_tag_script();
    let r2: Int = html_tag_script();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_WEB_ECOSYSTEM_HTML_SANITIZER_OO_STUB_OK");
}
