// # Product prove std/robot/auv/auv_mpc_trajectory_tracker.oo
//
// Logline: Fail-closed emit+gcc+run asserts mpc_horizon_config_default is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/auv/auv_mpc_trajectory_tracker.oo.
//
// Beats:
//   1. Call mpc_horizon_config_default twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_mpc_horizon_config_default: PASSED only if they match.

import "std/app/robot/auv/auv_mpc_trajectory_tracker.oo";

pub fn main() {
    let auv_mpc_trajectory_tracker_mark: Int = 0;
    if auv_mpc_trajectory_tracker_mark != 0 { process_exit(1); }
    let a: MpcHorizonConfig = mpc_horizon_config_default();
    let b: MpcHorizonConfig = mpc_horizon_config_default();
    if (a).horizon_steps != (b).horizon_steps {
        println("FAIL mpc_horizon_config_default");
        process_exit(1);
    }
    println("prod_mpc_horizon_config_default: PASSED");
}
