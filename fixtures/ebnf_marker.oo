// # EBNF residual marker
//
// Logline: Name the EBNF alignment surface without enforcing it.
//
// Beats:
//   1. Keep the EBNF residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker — see bootstrap/EBNF_ALIGN.oot
// EBNF: residual
pub fn main() { println(0); }
