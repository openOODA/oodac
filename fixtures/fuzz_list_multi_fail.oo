// # List arity-2 fuzz fail
//
// Logline: Return y while ensuring result == x so the list multi rail fails.
//
// Beats:
//   1. Keep FUZZ_DOMAIN list and target wrong 0 6.
//   2. Ensure result == x but return y.
//   3. Leave main empty; the fuzzer drives the target.

// M137 pure List arity-2 multi-arg fail-rail
// FUZZ_DOMAIN: list
// FUZZ_TARGET: wrong 0 6
// FUZZ_ENSURES: wrong result == x
fn wrong(x: List[Int], y: List[Int]) -> List[Int] {
    return y;
}

pub fn main() {
}
