import "std/sec/attestation_stubs.oo";

pub fn main() {
    let attestation_stubs_mark: Int = 0;
    if attestation_stubs_mark != 0 { process_exit(1); }
    let a: Result[String, String] = attestation_read_hardware_tpm_pcr(0, 0);
    let b: Result[String, String] = attestation_read_hardware_tpm_pcr(0, 0);
    if a != b { process_exit(1); }
    println("prod_attestation_stubs: PASSED");
}
