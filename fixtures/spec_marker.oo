// # Mark spec residual
//
// Logline: Print 0 as a named SPEC residual marker with no enforcement.
//
// Setup: Test fixture. Keep the SPEC tag and all code exact.
//
// Beats:
//   1. Carry the SPEC residual tag.
//   2. Keep main as a marker body.
//   3. Print 0.
//
// Residual marker — see bootstrap/SPEC_DEPTH.oot
// SPEC: residual
pub fn main() { println(0); }
