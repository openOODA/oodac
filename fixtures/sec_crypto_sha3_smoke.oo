// # Auto-generated signature-aware smoke stub for std/sec/crypto/sha3.oo.
//
// Logline: Re-exports SHA-3 data types, state constructors, engine lifecycle, and fail-closed stubs.
// Setup: Pure module shim importing companion submodules.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_str.
//   4. Print STD_SEC_CRYPTO_SHA3_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/sec/hash/sha3.oo";

fn expect_eq_str(label: String, got: String, want: String) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got + " want=" + want);
    return false;
}

pub fn main() {
    let r: String = shim_marker_sha3_checksum();
    let r2: String = shim_marker_sha3_checksum();
    if !expect_eq_str("call1 ok", r, r) { return; }
    if !expect_eq_str("call2 ok", r2, r2) { return; }
    println("STD_SEC_CRYPTO_SHA3_OO_STUB_OK");
}
