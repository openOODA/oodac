// # Auto-generated signature-aware smoke stub for std/hw/silicon/aarch64_neon.oo.
//
// Logline: Encodes 128-bit Neon vector arithmetic and Q-register pair load/store as 32-bit little-endian words.
// Setup: Vector registers v0–v31 and q0–q31 share indices 0..31. Integer MUL.2D is not in AdvSIMD; MUL.2D emits FMUL.2D.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_ARCH_AARCH64_AARCH64_NEON_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/hw/silicon/aarch64_neon.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = neon_rd(0);
    let r2: Int = neon_rd(0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_ARCH_AARCH64_AARCH64_NEON_OO_STUB_OK");
}
