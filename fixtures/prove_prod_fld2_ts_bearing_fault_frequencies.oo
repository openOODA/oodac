// # Product prove std/robot/predictive/ts_bearing_fault_frequencies.oo
//
// Logline: Fail-closed emit+gcc+run asserts int_to_float is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/predictive/ts_bearing_fault_frequencies.oo.
//
// Beats:
//   1. Call int_to_float twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ts_bearing_fault_frequencies: PASSED only if they match.

import "std/app/robot/predictive/ts_bearing_fault_frequencies.oo";

pub fn main() {
    let ts_bearing_fault_frequencies_mark: Int = 0;
    if ts_bearing_fault_frequencies_mark != 0 {
        process_exit(1);
    }
    let ts_bearing_fault_frequencies_a: Float = int_to_float(1);
    let ts_bearing_fault_frequencies_b: Float = int_to_float(1);
    if ts_bearing_fault_frequencies_a != ts_bearing_fault_frequencies_b {
        println("FAIL int_to_float");
        process_exit(1);
    }
    println("prod_ts_bearing_fault_frequencies: PASSED");
}
