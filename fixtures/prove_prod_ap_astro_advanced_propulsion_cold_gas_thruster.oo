// # Product prove std/phys/astro_advanced/propulsion_cold_gas_thruster.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/astro_advanced/propulsion_cold_gas_thruster.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/astro_advanced/propulsion_cold_gas_thruster.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_propulsion_cold_gas_thruster: PASSED only if they match.

import "std/science/phys/relativistic/propulsion_cold_gas_thruster.oo";

pub fn main() {
    let propulsion_cold_gas_thruster_a = cold_gas_isp();
    let propulsion_cold_gas_thruster_b = cold_gas_isp();
    if propulsion_cold_gas_thruster_a != propulsion_cold_gas_thruster_b {
        println("FAIL propulsion_cold_gas_thruster");
        process_exit(1);
    }
    println("prod_ap_propulsion_cold_gas_thruster: PASSED");
}
