// # Auto-generated signature-aware smoke stub for std/text/case_converter.oo.
//
// Logline: Identifier casing conversions: snake_case, camelCase, PascalCase, kebab-case, screaming_snake_case, and Title Case.
// Setup: Pure string manipulation module converting identifiers across naming conventions.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_TEXT_CASE_CONVERTER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/core/text/case_converter.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: String = char_to_lower("x");
    let r2: String = char_to_lower("x");
    if !expect_eq_int("r len", r.len(), r.len()) { return; }
    if !expect_eq_int("r2 len", r2.len(), r2.len()) { return; }
    println("STD_TEXT_CASE_CONVERTER_OO_STUB_OK");
}
