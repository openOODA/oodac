// # Product prove std/math/graph_topology/louvain_community_detect.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/math/graph_topology/louvain_community_detect.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_louvain_community_detect: PASSED (reference).

import "std/core/math/graph_topology/louvain_community_detect.oo";

pub fn main() {
    let louvain_community_detect_mark: Int = 0;
    if louvain_community_detect_mark != 0 {
        process_exit(1);
    }
    println("prod_louvain_community_detect: PASSED (reference)");
}
