// # Product prove std/phys/space_propulsion/space_propulsion_constants.oo
//
// Logline: Fail-closed emit+gcc+run asserts const_pi is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/space_propulsion/space_propulsion_constants.oo.
//
// Beats:
//   1. Call const_pi twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_space_propulsion_constants: PASSED only if they match.

import "std/science/phys/relativistic/space_propulsion_constants.oo";

pub fn main() {
    let space_propulsion_constants_mark: Int = 0;
    if space_propulsion_constants_mark != 0 {
        process_exit(1);
    }
    let a: Float = const_pi();
    let b: Float = const_pi();
    if a != b {
        println("FAIL const_pi");
        process_exit(1);
    }
    println("prod_space_propulsion_constants: PASSED");
}
