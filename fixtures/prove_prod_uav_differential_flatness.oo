// # Product prove std/phys/uav_flight/uav_differential_flatness.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/phys/uav_flight/uav_differential_flatness.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_uav_differential_flatness: PASSED (reference).

import "std/science/phys/uav_flight/uav_differential_flatness.oo";

pub fn main() {
    let uav_differential_flatness_mark: Int = 0;
    if uav_differential_flatness_mark != 0 {
        process_exit(1);
    }
    println("prod_uav_differential_flatness: PASSED (reference)");
}
