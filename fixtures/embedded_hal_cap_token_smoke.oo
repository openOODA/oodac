// # Auto-generated signature-aware smoke stub for std/hw/bare_metal/cap_token.oo.
//
// Logline: Bit-exact 256-bit capability descriptors, HMAC seal validation, and monotonic rights attenuation.
// Setup: Declared capability: None. Pure mathematical bitmask operations and cryptographic token validation.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_EMBEDDED_HAL_CAP_TOKEN_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/hw/bare_metal/cap_token.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = cap_bitmask_and(0, 0);
    let r2: Int = cap_bitmask_and(0, 0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_EMBEDDED_HAL_CAP_TOKEN_OO_STUB_OK");
}
