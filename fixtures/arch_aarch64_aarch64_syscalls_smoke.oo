// # Auto-generated signature-aware smoke stub for std/hw/silicon/aarch64_syscalls.oo.
//
// Logline: Emits svc #0 sequences that load Linux AArch64 syscall numbers into x8 and arguments into x0..x5.
// Setup: Uses AAPCS64 argument registers and the encoder MOVZ plus SVC helpers. Numbers are the Linux aarch64 table.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_ARCH_AARCH64_AARCH64_SYSCALLS_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/hw/silicon/aarch64_syscalls.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = aarch64_sys_read();
    let r2: Int = aarch64_sys_read();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_ARCH_AARCH64_AARCH64_SYSCALLS_OO_STUB_OK");
}
