// # Product prove std/econ/fin/orderbook.oo
//
// Logline: Fail-closed emit+gcc+run asserts shim_marker_orderbook_init holds a real value or is deterministic.
//
// Setup: Isolated emit-c. Imports std/econ/fin/orderbook.oo.
//
// Beats:
//   1. Call shim_marker_orderbook_init using the orderbook ident.
//   2. Exit 1 if the assert misses.
//   3. Print prod_orderbook: PASSED only if asserts hold.

import "std/science/econ/fin/orderbook.oo";

pub fn main() {
    let orderbook_mark: Int = 0;
    if orderbook_mark != 0 {
        process_exit(1);
    }
    let obj: OrderBook = shim_marker_orderbook_init();
    let orderbook_a: Int = (obj).bid_count;
    let orderbook_b: Int = (obj).bid_count;
    if orderbook_a != orderbook_b {
        println("FAIL shim_marker_orderbook_init");
        process_exit(1);
    }
    println("prod_orderbook: PASSED");
    process_exit(0);
}
