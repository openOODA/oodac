// # Product prove std/data/graph/db_graph_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts db_graph_fail_closed is deterministic.
//
// Setup: Isolated emit-c. Imports std/data/graph/db_graph_stubs.oo.
//
// Beats:
//   1. Call db_graph_fail_closed twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_db_graph_stubs: PASSED only if they match.

import "std/app/data/graph/db_graph_stubs.oo";

pub fn main() {
    let db_graph_stubs_mark: Int = 0;
    if db_graph_stubs_mark != 0 {
        process_exit(1);
    }
    let a: Bool = db_graph_fail_closed("x", 0);
    let b: Bool = db_graph_fail_closed("x", 0);
    if a != b {
        println("FAIL db_graph_fail_closed");
        process_exit(1);
    }
    println("prod_db_graph_stubs: PASSED");
}
