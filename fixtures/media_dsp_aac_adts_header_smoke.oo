// # Auto-generated signature-aware smoke stub for std/ui/media_dsp/aac_adts_header.oo.
//
// Logline: Decodes Advanced Audio Coding (AAC) ADTS fixed and variable sync headers, audio profiles, and frame sizes.
// Setup: Implements ISO/IEC 13818-7 / 14496-3 ADTS streaming protocol for MPEG-2/4 AAC audio bitstreams.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_MEDIA_DSP_AAC_ADTS_HEADER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/media_dsp/aac_adts_header.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = aac_lookup_sampling_rate(0);
    let r2: Int = aac_lookup_sampling_rate(0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_MEDIA_DSP_AAC_ADTS_HEADER_OO_STUB_OK");
}
