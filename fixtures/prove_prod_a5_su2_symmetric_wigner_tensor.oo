// # Product prove std/phys/quantum_tensor_nets/su2_symmetric_wigner_tensor.oo
//
// Logline: Fail-closed emit+gcc+run asserts su2_irrep_new is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/quantum_tensor_nets/su2_symmetric_wigner_tensor.oo.
//
// Beats:
//   1. Call su2_irrep_new twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_su2_symmetric_wigner_tensor: PASSED only if they match.

import "std/science/phys/quantum/su2_symmetric_wigner_tensor.oo";

pub fn main() {
    let su2_symmetric_wigner_tensor_mark: Int = 0;
    if su2_symmetric_wigner_tensor_mark != 0 {
        process_exit(1);
    }
    let a: Su2Irrep = su2_irrep_new(0);
    let b: Su2Irrep = su2_irrep_new(0);
    if (a).two_j != (b).two_j {
        println("FAIL su2_irrep_new");
        process_exit(1);
    }
    println("prod_su2_symmetric_wigner_tensor: PASSED");
}
