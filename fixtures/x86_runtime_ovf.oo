// # emit-x86 runtime concatenation overflow check
//
// Logline: Runtime string concatenation that dynamically checks buffer bounds (4096 bytes) and exits safely.

fn cat(a: String, b: String) -> String {
    return a + b;
}

pub fn main() {
    println(cat("hello ", "world"));
}
