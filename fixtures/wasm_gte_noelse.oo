// # Const if 2 >= 1 println no else
//
// Logline: Print gte-ne when 2 >= 1 and there is no else branch.
//
// Setup: No imports. One public main.
//
// Beats:
//   1. Declare public main.
//   2. If 2 >= 1 print gte-ne.
//   3. Leave no else and no other statements.

pub fn main() {
    if 2 >= 1 {
        println("gte-ne");
    }
}
