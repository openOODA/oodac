// # Import a colliding free name
//
// Logline: Import two modules that both export shared_name and print the winner.
//
// Setup: Test fixture. Keep all code exact. Residual: last import may win.
//
// Beats:
//   1. Import both leaves.
//   2. Call shared_name(1).
//   3. Print the result.
//
// SPRINT Issue #12 / #10 residual — duplicate free-name across two imports.
// HONESTY (tip host): oodac check currently ACCEPTS this (last import wins / silent merge).
// Not a hard product error yet — not asserted as FAIL until collision policy is defined.
// This is NOT a pass rail: smoke records RESIDUAL only (no xfail-fail, no silent claim of deny).
// If check starts ERRing on collisions, promote to typecheck_bugs/fail and update smoke.
import "./mod_a.oo";
import "./mod_b.oo";

pub fn main() {
    println(shared_name(1));
}
