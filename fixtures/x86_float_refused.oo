// # emit-x86 float runtime call must be refused (no const-fold)
pub fn add_floats(a: Float, b: Float) -> Float {
    return a + b;
}
pub fn main() {
    let x: Float = add_floats(1.0, 2.0);
    println(x);
}
