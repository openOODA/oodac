// # Auto-generated signature-aware smoke stub for std/markup/protobuf_wire.oo.
//
// Logline: Varint LEB128 serialization, ZigZag signed mapping, and tag packing for Protobuf wire streams.
// Setup: Pure binary protocol serializer. Encodes and decodes Protocol Buffers wire types and tags.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_MARKUP_PROTOBUF_WIRE_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/core/format/protobuf_wire.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = pb_wire_varint();
    let r2: Int = pb_wire_varint();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_MARKUP_PROTOBUF_WIRE_OO_STUB_OK");
}
