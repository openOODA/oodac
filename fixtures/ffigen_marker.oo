// # FFI-gen residual marker
//
// Logline: Name the FFI_GEN surface without enforcing it.
//
// Beats:
//   1. Keep the FFI_GEN residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/FFI_GEN.oot
// FFI_GEN: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
