import "std/sec/redteam_stubs.oo";

pub fn main() {
    let redteam_stubs_mark: Int = 0;
    if redteam_stubs_mark != 0 { process_exit(1); }
    let a: Bool = redteam_inject_kernel_shellcode(0, 0);
    let b: Bool = redteam_inject_kernel_shellcode(0, 0);
    if a != b { process_exit(1); }
    println("prod_redteam_stubs: PASSED");
}
