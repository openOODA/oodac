// # Product prove std/bio/genomics/minimap2_minimizer_chain.oo
//
// Logline: Fail-closed emit+check asserts minimap_calc_gap_cost is deterministic.
//
// Setup: Isolated emit-c. Imports std/bio/genomics/minimap2_minimizer_chain.oo.
//
// Beats:
//   1. Call minimap_calc_gap_cost twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_minimap2_minimizer_chain: PASSED only if they match.

import "std/science/bio/genomics/minimap2_minimizer_chain.oo";

pub fn main() {
    let minimap2_minimizer_chain_mark: Int = 0;
    if minimap2_minimizer_chain_mark != 0 { process_exit(1); }
    let a: Int = minimap_calc_gap_cost(2, 3, 4, 1);
    let b: Int = minimap_calc_gap_cost(2, 3, 4, 1);
    if a != b { process_exit(1); }
    println("prod_minimap2_minimizer_chain: PASSED");
}
