// # Product prove std/ui/web_ecosystem/markdown_to_html.oo
//
// Logline: Fail-closed emit+gcc+run asserts markdown_to_html is deterministic.
//
// Setup: Isolated emit-c. Imports std/ui/web_ecosystem/markdown_to_html.oo.
//
// Beats:
//   1. Call markdown_to_html twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_markdown_to_html: PASSED only if they match.

import "std/app/ui/web_ecosystem/markdown_to_html.oo";

pub fn main() {
    let a: String = markdown_to_html("x");
    let b: String = markdown_to_html("x");
    if a != b {
        println("FAIL markdown_to_html");
        process_exit(1);
    }
    println("prod_markdown_to_html: PASSED");
}
