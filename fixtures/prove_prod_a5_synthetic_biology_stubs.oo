// # Product prove std/bio/synthetic/synthetic_biology_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts synbio_stub_gpu_ssa_throughput_trajectories_per_sec is deterministic.
//
// Setup: Isolated emit-c. Imports std/bio/synthetic/synthetic_biology_stubs.oo.
//
// Beats:
//   1. Call synbio_stub_gpu_ssa_throughput_trajectories_per_sec twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_synthetic_biology_stubs: PASSED only if they match.

import "std/science/bio/synthetic/synthetic_biology_stubs.oo";

pub fn main() {
    let synthetic_biology_stubs_mark: Int = 0;
    if synthetic_biology_stubs_mark != 0 {
        process_exit(1);
    }
    let a: Float = synbio_stub_gpu_ssa_throughput_trajectories_per_sec(0);
    let b: Float = synbio_stub_gpu_ssa_throughput_trajectories_per_sec(0);
    if a != b {
        println("FAIL synbio_stub_gpu_ssa_throughput_trajectories_per_sec");
        process_exit(1);
    }
    println("prod_synthetic_biology_stubs: PASSED");
}
