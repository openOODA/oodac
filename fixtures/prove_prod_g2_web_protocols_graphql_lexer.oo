// # Product prove std/net/web_protocols/graphql_lexer.oo
//
// Logline: Fail-closed emit+gcc+run asserts graphql_is_whitespace is a real value.
//
// Setup: Isolated emit-c. Imports std/net/web_protocols/graphql_lexer.oo. Avoids pointer field dot and Result unwrap.
//
// Beats:
//   1. Call graphql_is_whitespace twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_graphql_lexer: PASSED only if they match.

import "std/net/protocol/graphql_lexer.oo";

pub fn main() {
    let g2_a = graphql_is_whitespace(2);
    let g2_b = graphql_is_whitespace(2);
    if g2_a != g2_b {
        println("FAIL graphql_is_whitespace");
        process_exit(1);
    }
    println("prod_g2_graphql_lexer: PASSED");
}
