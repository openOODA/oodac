// # Mark shadow residual
//
// Logline: Print 0 as a named SHADOW residual marker with no enforcement.
//
// Setup: Test fixture. Keep the SHADOW tag and all code exact.
//
// Beats:
//   1. Carry the SHADOW residual tag.
//   2. Name the surface only.
//   3. Print 0 from main.
//
// Residual marker fixture — see bootstrap/SHADOW_STATE.oot
// SHADOW: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
