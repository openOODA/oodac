// # Callgraph residual marker
//
// Logline: Name the callgraph crypto surface without enforcing it.
//
// Beats:
//   1. Keep the CALLGRAPH residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/CALLGRAPH_CRYPTO.oot
// CALLGRAPH: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
