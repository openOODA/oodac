// # Product prove std/net/edge/inference_pipeline.oo
//
// Logline: Fail-closed emit+gcc+run asserts inference_pipeline_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/net/edge/inference_pipeline.oo.
//
// Beats:
//   1. Call inference_pipeline_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_inference_pipeline: PASSED only if it matches.

import "std/net/mesh/inference_pipeline.oo";

pub fn main() {
    let a: Int = inference_pipeline_probe();
    if a != 0 {
        println("FAIL inference_pipeline_probe");
        process_exit(1);
    }
    println("prod_nc_inference_pipeline: PASSED");
}
