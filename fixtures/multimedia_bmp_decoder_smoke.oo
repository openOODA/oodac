// # Auto-generated signature-aware smoke stub for std/ui/multimedia/bmp_decoder.oo.
//
// Logline: Parses a 14-byte BITMAPFILEHEADER and 40-byte DIB header for uncompressed 24-bit and 32-bit RGB with 4-byte row padding.
// Setup: Pure. Bytes are List[Int] 0..255 little-endian. Compressed or truncated headers are Err.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_MULTIMEDIA_BMP_DECODER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/multimedia/bmp_decoder.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = bmp_decoder_stride(0, 0);
    let r2: Int = bmp_decoder_stride(0, 0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_MULTIMEDIA_BMP_DECODER_OO_STUB_OK");
}
