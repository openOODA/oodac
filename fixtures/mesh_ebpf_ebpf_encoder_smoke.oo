// # Auto-generated signature-aware smoke stub for std/compile/mesh_ebpf/ebpf_encoder.oo.
//
// Logline: Packs standard 8-byte little-endian eBPF insns and builds ALU64, memory, jump, EXIT, and LSM filter streams.
// Setup: Imports ebpf_types.oo. Pure. No capability tokens.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_MESH_EBPF_EBPF_ENCODER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/compile/mesh_ebpf/ebpf_encoder.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = ebpf_encoder_pack(0, 0, 0, 0, 0);
    let r2: Int = ebpf_encoder_pack(0, 0, 0, 0, 0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_MESH_EBPF_EBPF_ENCODER_OO_STUB_OK");
}
