// # Product prove std/phys/aero/aerodynamics_ops.oo
//
// Logline: Fail-closed emit+gcc+run asserts math_pi is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/aero/aerodynamics_ops.oo.
//
// Beats:
//   1. Call math_pi twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_aerodynamics_ops: PASSED only if they match.

import "std/app/combat/aerodynamics_ops.oo";

pub fn main() {
    let aerodynamics_ops_mark: Int = 0;
    if aerodynamics_ops_mark != 0 {
        process_exit(1);
    }
    let a: Float = math_pi();
    let b: Float = math_pi();
    if a != b {
        println("FAIL math_pi");
        process_exit(1);
    }
    println("prod_g2_aerodynamics_ops: PASSED");
}
