// # Product prove std/sec/net/jwt_token_config.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/sec/net/jwt_token_config.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/sec/net/jwt_token_config.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_jwt_token_config: PASSED only if they match.

import "std/sec/net/jwt_token_config.oo";

pub fn main() {
    let jwt_token_config_a = __dummy_std_src_ooda_sec_net_jwt_token_config_oo();
    let jwt_token_config_b = __dummy_std_src_ooda_sec_net_jwt_token_config_oo();
    if jwt_token_config_a != jwt_token_config_b {
        println("FAIL jwt_token_config");
        process_exit(1);
    }
    println("prod_ap_jwt_token_config: PASSED");
}
