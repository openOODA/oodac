// # String arity-3 weak attack
//
// Logline: Offer a three-String target so the string multi-arg rail fail-closes.
//
// Beats:
//   1. Keep FUZZ_DOMAIN string and target three_str 0 6.
//   2. Take three Strings and return x.
//   3. Path A only samples arity-2, so this must fail closed.

// Weak: String multi arity≥3 fail-closed (M106 path only arity-2)
// FUZZ_DOMAIN: string
// FUZZ_TARGET: three_str 0 6
// FUZZ_ENSURES: three_str result == x
fn three_str(x: String, y: String, z: String) -> String {
    return x;
}

pub fn main() {
}
