// # Concurrency residual marker
//
// Logline: Name the concurrency surface without enforcing it.
//
// Beats:
//   1. Keep the CONCURRENCY residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/CONCURRENCY.oot
// CONCURRENCY: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
