// # Product prove std/ai/multimodal/unet_upsample_block.oo
//
// Logline: Fail-closed emit+gcc+run asserts unet_upsample_marker is deterministic.
//
// Setup: Isolated emit-c. Imports std/ai/multimodal/unet_upsample_block.oo.
//
// Beats:
//   1. Call unet_upsample_marker twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_unet_upsample_block: PASSED only if they match.

import "std/app/ai/multimodal/unet_upsample_block.oo";

pub fn main() {
    let unet_upsample_block_mark: Int = 0;
    if unet_upsample_block_mark != 0 {
        process_exit(1);
    }
    let a: Int = unet_upsample_marker();
    let b: Int = unet_upsample_marker();
    if a != b {
        println("FAIL unet_upsample_marker");
        process_exit(1);
    }
    println("prod_g2_unet_upsample_block: PASSED");
}
