// # Product prove std/phys/astrophysics_relativity/stellar_lane_emden_polytrope.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/astrophysics_relativity/stellar_lane_emden_polytrope.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/astrophysics_relativity/stellar_lane_emden_polytrope.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_stellar_lane_emden_polytrope: PASSED only if they match.

import "std/science/phys/relativistic/stellar_lane_emden_polytrope.oo";

pub fn main() {
    let stellar_lane_emden_polytrope_a = polytrope_derivatives(0.0, 0.0, 0.0, 0.0);
    let stellar_lane_emden_polytrope_b = polytrope_derivatives(0.0, 0.0, 0.0, 0.0);
    if stellar_lane_emden_polytrope_a != stellar_lane_emden_polytrope_b {
        println("FAIL stellar_lane_emden_polytrope");
        process_exit(1);
    }
    println("prod_ap_stellar_lane_emden_polytrope: PASSED");
}
