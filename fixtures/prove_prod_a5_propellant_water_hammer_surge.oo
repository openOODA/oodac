// # Product prove std/phys/space_propulsion/propellant_water_hammer_surge.oo
//
// Logline: Fail-closed emit+gcc+run asserts propellant_line_spec_default_lox is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/space_propulsion/propellant_water_hammer_surge.oo.
//
// Beats:
//   1. Call propellant_line_spec_default_lox twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_propellant_water_hammer_surge: PASSED only if they match.

import "std/science/phys/relativistic/propellant_water_hammer_surge.oo";

pub fn main() {
    let propellant_water_hammer_surge_mark: Int = 0;
    if propellant_water_hammer_surge_mark != 0 {
        process_exit(1);
    }
    let a: PropellantLineSpec = propellant_line_spec_default_lox();
    let b: PropellantLineSpec = propellant_line_spec_default_lox();
    if (a).pipe_diameter_m != (b).pipe_diameter_m {
        println("FAIL propellant_line_spec_default_lox");
        process_exit(1);
    }
    println("prod_propellant_water_hammer_surge: PASSED");
}
