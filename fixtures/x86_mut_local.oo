// # emit-x86 mutable locals in stack frame
//
// Logline: Allocate mut local on stack frame, reassign value, and print via itoa.

pub fn main() {
    let mut x: Int = 1;
    x = 2;
    println(x);
}
