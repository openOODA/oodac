// # Auto-generated signature-aware smoke stub for std/hash/sha256.oo.
//
// Logline: Forwards a digest to crypto_sha256_internal in the product crypto module.
// Setup: Product SHA-256 lives in std/core/crypto.oo (CHS, not FIPS).
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_HASH_SHA256_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/sec/hash/sha256.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: String = hash_sha256_forward("x");
    let r2: String = hash_sha256_forward("x");
    if !expect_eq_int("r len", r.len(), r.len()) { return; }
    if !expect_eq_int("r2 len", r2.len(), r2.len()) { return; }
    println("STD_HASH_SHA256_OO_STUB_OK");
}
