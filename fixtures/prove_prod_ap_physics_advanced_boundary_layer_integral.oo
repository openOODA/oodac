// # Product prove std/phys/advanced/boundary_layer_integral.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/advanced/boundary_layer_integral.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/advanced/boundary_layer_integral.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_boundary_layer_integral: PASSED only if they match.

import "std/science/phys/classical/boundary_layer_integral.oo";

pub fn main() {
    let boundary_layer_integral_a = turbulent_flat_plate_theta(0.0, 0.0);
    let boundary_layer_integral_b = turbulent_flat_plate_theta(0.0, 0.0);
    if boundary_layer_integral_a != boundary_layer_integral_b {
        println("FAIL boundary_layer_integral");
        process_exit(1);
    }
    println("prod_ap_boundary_layer_integral: PASSED");
}
