// # GPU residual marker
//
// Logline: Name the GPU surface without enforcing device shaders.
//
// Beats:
//   1. Keep the GPU residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/GPU_NPU.oot
// GPU: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
