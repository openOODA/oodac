// # Product prove std/phys/astro_advanced/attitude_quest_algorithm.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/astro_advanced/attitude_quest_algorithm.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/astro_advanced/attitude_quest_algorithm.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_attitude_quest_algorithm: PASSED only if they match.

import "std/science/phys/relativistic/attitude_quest_algorithm.oo";

pub fn main() {
    let attitude_quest_algorithm_a = quest_yaw(0);
    let attitude_quest_algorithm_b = quest_yaw(0);
    if attitude_quest_algorithm_a != attitude_quest_algorithm_b {
        println("FAIL attitude_quest_algorithm");
        process_exit(1);
    }
    println("prod_ap_attitude_quest_algorithm: PASSED");
}
