// # Product prove std/data/grid/wind_turbine.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/data/grid/wind_turbine.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_wind_turbine: PASSED (reference).

import "std/app/data/grid/wind_turbine.oo";

pub fn main() {
    let wind_turbine_mark: Int = 0;
    if wind_turbine_mark != 0 {
        process_exit(1);
    }
    println("prod_wind_turbine: PASSED (reference)");
}
