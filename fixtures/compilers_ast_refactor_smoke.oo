// # Auto-generated signature-aware smoke stub for std/compile/compilers/ast_refactor.oo.
//
// Logline: Abstract Syntax Tree refactoring transformations, symbol renames, function extraction, and workspace edits.
// Setup: Imports lsp_protocol.oo with capability-gated &FsReadCap and &FsWriteCap for file transformations.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_COMPILERS_AST_REFACTOR_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/compile/compilers/ast_refactor.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = refactor_kind_rename();
    let r2: Int = refactor_kind_rename();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_COMPILERS_AST_REFACTOR_OO_STUB_OK");
}
