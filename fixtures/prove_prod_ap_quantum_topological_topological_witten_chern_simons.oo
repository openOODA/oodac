// # Product prove std/phys/quantum_topological/topological_witten_chern_simons.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/quantum_topological/topological_witten_chern_simons.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/quantum_topological/topological_witten_chern_simons.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_topological_witten_chern_simons: PASSED only if they match.

import "std/science/phys/quantum/topological_witten_chern_simons.oo";

pub fn main() {
    let topological_witten_chern_simons_a = int_to_float(0);
    let topological_witten_chern_simons_b = int_to_float(0);
    if topological_witten_chern_simons_a != topological_witten_chern_simons_b {
        println("FAIL topological_witten_chern_simons");
        process_exit(1);
    }
    println("prod_ap_topological_witten_chern_simons: PASSED");
}
