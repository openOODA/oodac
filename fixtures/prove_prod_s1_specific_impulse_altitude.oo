// # Product prove std/phys/space_propulsion/specific_impulse_altitude.oo
//
// Logline: Fail-closed emit+gcc+run asserts atmosphere_pressure_at_alt is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/space_propulsion/specific_impulse_altitude.oo.
//
// Beats:
//   1. Call atmosphere_pressure_at_alt twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_specific_impulse_altitude: PASSED only if they match.

import "std/science/phys/relativistic/specific_impulse_altitude.oo";

pub fn main() {
    let specific_impulse_altitude_mark: Int = 0;
    if specific_impulse_altitude_mark != 0 {
        process_exit(1);
    }
    let specific_impulse_altitude_a: Float = atmosphere_pressure_at_alt(1.0);
    let specific_impulse_altitude_b: Float = atmosphere_pressure_at_alt(1.0);
    if specific_impulse_altitude_a != specific_impulse_altitude_b {
        println("FAIL atmosphere_pressure_at_alt");
        process_exit(1);
    }
    println("prod_specific_impulse_altitude: PASSED");
}
