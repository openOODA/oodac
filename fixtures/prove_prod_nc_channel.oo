// # Product prove std/sync/channel.oo
//
// Logline: Fail-closed emit+gcc+run asserts channel_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/sync/channel.oo.
//
// Beats:
//   1. Call channel_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_channel: PASSED only if it matches.

import "std/net/sync/channel.oo";

pub fn main() {
    let a: Int = channel_probe();
    if a != 0 {
        println("FAIL channel_probe");
        process_exit(1);
    }
    println("prod_nc_channel: PASSED");
}
