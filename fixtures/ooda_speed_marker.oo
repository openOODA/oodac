// # Name the OODA-speed residual
//
// Logline: This marker names the OODA-speed residual surface without enforcing it.
//
// Beats:
//   1. Keep the residual marker line for honesty smokes.
//   2. Point at bootstrap/OODA_SPEED.oot as the surface doc.
//   3. Print zero so a named-only fixture still parses.

// Residual marker — see bootstrap/OODA_SPEED.oot
// OODA_SPEED: residual
pub fn main() { println(0); }
