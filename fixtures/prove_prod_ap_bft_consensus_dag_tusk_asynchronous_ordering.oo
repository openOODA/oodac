// # Product prove std/compile/bft_consensus/dag_tusk_asynchronous_ordering.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/compile/bft_consensus/dag_tusk_asynchronous_ordering.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/compile/bft_consensus/dag_tusk_asynchronous_ordering.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_dag_tusk_asynchronous_ordering: PASSED only if they match.

import "std/compile/bft_consensus/dag_tusk_asynchronous_ordering.oo";

pub fn main() {
    let dag_tusk_asynchronous_ordering_a = tusk_wave_to_leader_round(0);
    let dag_tusk_asynchronous_ordering_b = tusk_wave_to_leader_round(0);
    if dag_tusk_asynchronous_ordering_a != dag_tusk_asynchronous_ordering_b {
        println("FAIL dag_tusk_asynchronous_ordering");
        process_exit(1);
    }
    println("prod_ap_dag_tusk_asynchronous_ordering: PASSED");
}
