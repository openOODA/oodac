// # Product prove std/sec/crypto/rsa_pss_engine.oo
//
// Logline: Fail-closed emit+gcc+run asserts rsa_pss_engine_ready is 1.
//
// Setup: Isolated emit-c. Imports std/sec/crypto/rsa_pss_engine.oo.
//
// Beats:
//   1. rsa_pss_engine_ready() is 1.
//   2. Exit 1 on miss. Print prod_ap_rsa_pss_engine: PASSED only if it holds.

import "std/sec/asymmetric/rsa_pss_engine.oo";

pub fn main() {
    let rsa_pss_engine_a = rsa_pss_engine_ready();
    if rsa_pss_engine_a != 1 {
        println("FAIL rsa_pss_engine");
        process_exit(1);
    }
    println("prod_ap_rsa_pss_engine: PASSED");
}
