// # Compare an Int field with false
//
// Logline: Compare State.n with false so typecheck must emit an incompatible-operand error.
//
// Setup: Test fixture. Keep all code exact.
//
// Beats:
//   1. Define State with Bool flag and Int n.
//   2. Compare s.n == false in score.
//   3. Leave main empty.
//
// SPRINT Issue #12 / #11 fail-closed — Int field compared with false.
// STATUS: expected ERR type (incompatible operands). Must not silent-pass.
type State = struct {
    flag: Bool,
    n: Int,
};

pub fn score(s: State) -> Int {
    if s.n == false {
        return 0;
    }
    return 1;
}

pub fn main() {}
