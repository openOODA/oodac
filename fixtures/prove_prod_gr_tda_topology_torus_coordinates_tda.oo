// # Product prove std/math/tda_topology/torus_coordinates_tda.oo
//
// Logline: Fail-closed emit+gcc+run asserts torus_angular_diff is a real value.
//
// Setup: Isolated emit-c. Imports std/math/tda_topology/torus_coordinates_tda.oo. Avoids String != and Result unwrap.
//
// Beats:
//   1. Call torus_angular_diff twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_gr_torus_coordinates_tda: PASSED only if they match.

import "std/core/math/tda_topology/torus_coordinates_tda.oo";

pub fn main() {
    let g0_a = torus_angular_diff(0.0, 0.0);
    let g0_b = torus_angular_diff(0.0, 0.0);
    if g0_a != g0_b {
        println("FAIL torus_angular_diff");
        process_exit(1);
    }
    println("prod_gr_torus_coordinates_tda: PASSED");
}
