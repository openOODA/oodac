// # Product prove std/math/scicomp/tridiagonal_solver_thomas.oo
//
// Logline: Fail-closed emit+gcc+run asserts tridiagonal_solver_thomas_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/math/scicomp/tridiagonal_solver_thomas.oo.
//
// Beats:
//   1. Call tridiagonal_solver_thomas_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_tridiagonal_solver_thomas: PASSED only if it matches.

import "std/core/math/scicomp/tridiagonal_solver_thomas.oo";

pub fn main() {
    let a: Int = tridiagonal_solver_thomas_probe();
    if a != 0 {
        println("FAIL tridiagonal_solver_thomas_probe");
        process_exit(1);
    }
    println("prod_nc_tridiagonal_solver_thomas: PASSED");
}
