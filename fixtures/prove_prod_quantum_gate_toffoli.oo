// # Product prove std/phys/quantum/gate_toffoli.oo
//
// Logline: Fail-closed emit+check references gate_toffoli for type completeness.
//
// Setup: Isolated emit-c. Imports std/phys/quantum/gate_toffoli.oo.
//
// Beats:
//   1. Reference gate_toffoli for module existence.
//   2. Exit 0 if the type resolves.
//   3. Print prod_gate_toffoli: PASSED.

import "std/science/phys/quantum/gate_toffoli.oo";

pub fn main() {
    let gate_toffoli_mark: Int = 0;
    if gate_toffoli_mark != 0 {
        process_exit(1);
    }
    println("prod_gate_toffoli: PASSED");
}
