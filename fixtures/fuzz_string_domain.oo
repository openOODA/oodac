// # String-domain fuzz pass
//
// Logline: Identity-map a String so the pure string fuzz rail can pass.
//
// Beats:
//   1. Keep FUZZ_DOMAIN string and target id_str 0 8.
//   2. Ensure result == x and return x.
//   3. Leave main empty; the fuzzer drives the target.

// Pure String-domain fuzz pass (no Python).
// FUZZ_DOMAIN: string
// FUZZ_TARGET: id_str 0 8
// FUZZ_ENSURES: id_str result == x
fn id_str(x: String) -> String {
    return x;
}
pub fn main() {
}
