// # Auto-generated signature-aware smoke stub for std/compile/parsers/base58.oo.
//
// Logline: Encodes and decodes Base58 strings (the Bitcoin/IPFS alphabet) with leading-zero preservation, as used by Bitcoin addresses and IPFS CIDs.
// Setup: Pure value-returning helpers, no IO. The decoder returns Result[String, String] for invalid characters. Decimal literals in place of hex because the .oo lexer does not accept 0xNN.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_PARSERS_BASE58_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/compile/parsers/base58.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: String = base58_alphabet();
    let r2: String = base58_alphabet();
    if !expect_eq_int("r len", r.len(), r.len()) { return; }
    if !expect_eq_int("r2 len", r2.len(), r2.len()) { return; }
    println("STD_PARSERS_BASE58_OO_STUB_OK");
}
