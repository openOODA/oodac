// # Product prove std/robot/mpc/auto_mpc_control_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/robot/mpc/auto_mpc_control_stubs.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/mpc/auto_mpc_control_stubs.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_auto_mpc_control_stubs: PASSED only if they match.

import "std/app/robot/mpc/auto_mpc_control_stubs.oo";

pub fn main() {
    let auto_mpc_control_stubs_a = auto_hal_steer_write_stub(0.0, true);
    let auto_mpc_control_stubs_b = auto_hal_steer_write_stub(0.0, true);
    if auto_mpc_control_stubs_a != auto_mpc_control_stubs_b {
        println("FAIL auto_mpc_control_stubs");
        process_exit(1);
    }
    println("prod_ap_auto_mpc_control_stubs: PASSED");
}
