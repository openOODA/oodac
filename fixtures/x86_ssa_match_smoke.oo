// # emit-x86 3-arm Option match bind
pub fn main() {
    let x = Some(42);
    match x {
        Some(v) => { println(v); },
        None => { println(0); },
        Ok(w) => { println(w); }
    }
}
