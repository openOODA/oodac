// # Product prove std/phys/astro_advanced/frame_dragging_lense_thirring.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/astro_advanced/frame_dragging_lense_thirring.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/astro_advanced/frame_dragging_lense_thirring.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_frame_dragging_lense_thirring: PASSED only if they match.

import "std/science/phys/relativistic/frame_dragging_lense_thirring.oo";

pub fn main() {
    let frame_dragging_lense_thirring_a = lt_precession_mas();
    let frame_dragging_lense_thirring_b = lt_precession_mas();
    if frame_dragging_lense_thirring_a != frame_dragging_lense_thirring_b {
        println("FAIL frame_dragging_lense_thirring");
        process_exit(1);
    }
    println("prod_ap_frame_dragging_lense_thirring: PASSED");
}
