// # Product prove std/ai/multimodal/contrastive_loss_infonce.oo
//
// Logline: Fail-closed emit+gcc+run asserts contrastive_loss_infonce_marker is deterministic.
//
// Setup: Isolated emit-c. Imports std/ai/multimodal/contrastive_loss_infonce.oo.
//
// Beats:
//   1. Call contrastive_loss_infonce_marker twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_contrastive_loss_infonce: PASSED only if they match.

import "std/app/ai/multimodal/contrastive_loss_infonce.oo";

pub fn main() {
    let a: Int = contrastive_loss_infonce_marker();
    let b: Int = contrastive_loss_infonce_marker();
    if a != b {
        println("FAIL contrastive_loss_infonce_marker");
        process_exit(1);
    }
    println("prod_g2_contrastive_loss_infonce: PASSED");
}
