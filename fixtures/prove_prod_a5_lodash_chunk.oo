// # Product prove std/ui/web_ecosystem/lodash_chunk.oo
//
// Logline: Fail-closed emit+gcc+run asserts lodash_chunk_n is deterministic.
//
// Setup: Isolated emit-c. Imports std/ui/web_ecosystem/lodash_chunk.oo.
//
// Beats:
//   1. Call lodash_chunk_n twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_lodash_chunk: PASSED only if they match.

import "std/app/ui/web_ecosystem/lodash_chunk.oo";

pub fn main() {
    let a: Int = lodash_chunk_n(0, 0);
    let b: Int = lodash_chunk_n(0, 0);
    if a != b {
        println("FAIL lodash_chunk_n");
        process_exit(1);
    }
    println("prod_lodash_chunk: PASSED");
}
