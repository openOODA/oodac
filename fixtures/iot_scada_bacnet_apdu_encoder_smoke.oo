// # Auto-generated signature-aware smoke stub for std/robot/iot_scada/bacnet_apdu_encoder.oo.
//
// Logline: Encodes and parses BACnet Application Protocol Data Units (APDU) and service requests.
// Setup: Supports Confirmed-Request, Unconfirmed-Request, Simple-ACK, Complex-ACK, and Error PDUs.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_IOT_SCADA_BACNET_APDU_ENCODER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/robot/iot_scada/bacnet_apdu_encoder.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = bacnet_service_read_property();
    let r2: Int = bacnet_service_read_property();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_IOT_SCADA_BACNET_APDU_ENCODER_OO_STUB_OK");
}
