// # Product sync cyclic barrier prove
//
// Logline: Fail-closed emit+gcc+run of shipped std/sync/barrier_cyclic.oo.
//
// Setup: Product std/sync/barrier_cyclic.oo. Isolated emit-c+gcc+run.
//
// Beats:
//   1. New is not broken.
//   9. Exit 1 before PASSED on a miss.

import "std/net/sync/barrier_cyclic.oo";

pub fn main() {
    let b: CyclicBarrier = cyclic_barrier_new(4);
    if cyclic_barrier_is_broken(b) {
        println("FAIL cycbar broken");
        process_exit(1);
    }
    println("prod_cycbar: PASSED");
}
