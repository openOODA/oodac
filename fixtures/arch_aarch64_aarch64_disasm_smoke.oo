// # Auto-generated signature-aware smoke stub for std/hw/silicon/aarch64_disasm.oo.
//
// Logline: Disassembles 32-bit AArch64 instruction words into genuine AAPCS64 GAS assembly syntax.
// Setup: Pure .oo disassembler. Takes instruction words and emits standard GNU assembler lines.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_ARCH_AARCH64_AARCH64_DISASM_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/hw/silicon/aarch64_disasm.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: String = aarch64_disasm_inst(0, 0);
    let r2: String = aarch64_disasm_inst(0, 0);
    if !expect_eq_int("r len", r.len(), r.len()) { return; }
    if !expect_eq_int("r2 len", r2.len(), r2.len()) { return; }
    println("STD_ARCH_AARCH64_AARCH64_DISASM_OO_STUB_OK");
}
