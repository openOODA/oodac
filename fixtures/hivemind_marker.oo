// # Hivemind residual marker
//
// Logline: Name the hivemind surface without enforcing it.
//
// Beats:
//   1. Keep the HIVEMIND residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/HIVEMIND.oot
// HIVEMIND: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
