// # Product prove std/crypto/advanced/bn254_curve_g1.oo
//
// Logline: Fail-closed emit+gcc+run asserts bn254_g1_infinity is deterministic.
//
// Setup: Isolated emit-c. Imports std/crypto/advanced/bn254_curve_g1.oo.
//
// Beats:
//   1. Call bn254_g1_infinity twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_bn254_curve_g1: PASSED only if they match.

import "std/sec/zk/bn254_curve_g1.oo";

pub fn main() {
    let bn254_curve_g1_mark: Int = 0;
    if bn254_curve_g1_mark != 0 {
        process_exit(1);
    }
    let a: Bn254G1Affine = bn254_g1_infinity(0);
    let b: Bn254G1Affine = bn254_g1_infinity(0);
    if (a).is_infinity != (b).is_infinity {
        println("FAIL bn254_g1_infinity");
        process_exit(1);
    }
    println("prod_bn254_curve_g1: PASSED");
}
