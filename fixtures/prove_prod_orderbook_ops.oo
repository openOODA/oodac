// # Product prove std/econ/fin/orderbook_ops.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/econ/fin/orderbook_ops.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_orderbook_ops: PASSED (reference).

import "std/science/econ/fin/orderbook_ops.oo";

pub fn main() {
    let orderbook_ops_mark: Int = 0;
    if orderbook_ops_mark != 0 {
        process_exit(1);
    }
    println("prod_orderbook_ops: PASSED (reference)");
}
