// # Product prove std/data/vector_ann/db_vector_ann_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/data/vector_ann/db_vector_ann_stubs.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/data/vector_ann/db_vector_ann_stubs.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_db_vector_ann_stubs: PASSED only if they match.

import "std/app/data/vector_ann/db_vector_ann_stubs.oo";

pub fn main() {
    let db_vector_ann_stubs_a = db_vector_ann_fail_closed("x", 0);
    let db_vector_ann_stubs_b = db_vector_ann_fail_closed("x", 0);
    if db_vector_ann_stubs_a != db_vector_ann_stubs_b {
        println("FAIL db_vector_ann_stubs");
        process_exit(1);
    }
    println("prod_ap_db_vector_ann_stubs: PASSED");
}
