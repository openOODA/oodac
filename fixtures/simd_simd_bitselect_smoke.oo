// # Auto-generated signature-aware smoke stub for std/simd/simd_bitselect.oo.
//
// Logline: 32-bit and 64-bit SIMD conditional selection, bitwise multiplexing, and boolean mask blending.
// Setup: Pure math module. Vectorized branchless multiplexing using bitwise masks.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_SIMD_SIMD_BITSELECT_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/sec/simd/simd_bitselect.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = simd_bitselect_i32(0, 0, 0);
    let r2: Int = simd_bitselect_i32(0, 0, 0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_SIMD_SIMD_BITSELECT_OO_STUB_OK");
}
