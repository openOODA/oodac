// # Product prove std/robot/auv/active_disturbance_rejection_auv.oo
//
// Logline: Fail-closed emit+gcc+run asserts adrc_config_default_surge is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/auv/active_disturbance_rejection_auv.oo.
//
// Beats:
//   1. Call adrc_config_default_surge twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_adrc_config_default_surge: PASSED only if they match.

import "std/app/robot/auv/active_disturbance_rejection_auv.oo";

pub fn main() {
    let active_disturbance_rejection_auv_mark: Int = 0;
    if active_disturbance_rejection_auv_mark != 0 { process_exit(1); }
    let a: AdrcConfig = adrc_config_default_surge();
    let b: AdrcConfig = adrc_config_default_surge();
    if (a).omega_o_observer_bw != (b).omega_o_observer_bw {
        println("FAIL adrc_config_default_surge");
        process_exit(1);
    }
    println("prod_adrc_config_default_surge: PASSED");
}
