// # Product prove std/phys/advanced/propeller_blade_element_momentum.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/advanced/propeller_blade_element_momentum.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/advanced/propeller_blade_element_momentum.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_propeller_blade_element_momentum: PASSED only if they match.

import "std/science/phys/classical/propeller_blade_element_momentum.oo";

pub fn main() {
    let propeller_blade_element_momentum_a = bem_prandtl_tip_loss(0.0, 0.0, 0, 0.0);
    let propeller_blade_element_momentum_b = bem_prandtl_tip_loss(0.0, 0.0, 0, 0.0);
    if propeller_blade_element_momentum_a != propeller_blade_element_momentum_b {
        println("FAIL propeller_blade_element_momentum");
        process_exit(1);
    }
    println("prod_ap_propeller_blade_element_momentum: PASSED");
}
