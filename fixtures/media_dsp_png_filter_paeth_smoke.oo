// # Auto-generated signature-aware smoke stub for std/ui/media_dsp/png_filter_paeth.oo.
//
// Logline: Implements ISO/IEC 15948 PNG scanline reconstruction filters (None, Sub, Up, Average, Paeth) and inverse filtering.
// Setup: Exact byte-level filtering and de-filtering arithmetic with modulo 256 wraparound.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_MEDIA_DSP_PNG_FILTER_PAETH_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/media_dsp/png_filter_paeth.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = png_filter_type_none();
    let r2: Int = png_filter_type_none();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_MEDIA_DSP_PNG_FILTER_PAETH_OO_STUB_OK");
}
