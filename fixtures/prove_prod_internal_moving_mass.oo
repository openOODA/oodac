// # Product prove std/robot/auv/internal_moving_mass.oo
//
// Logline: Fail-closed emit+gcc+run asserts moving_mass_params_default_glider is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/auv/internal_moving_mass.oo.
//
// Beats:
//   1. Call moving_mass_params_default_glider twice.
//   2. Exit 1 if the two results differ.
//   3. Print prod_internal_moving_mass: PASSED only if they match.

import "std/app/robot/auv/internal_moving_mass.oo";

pub fn main() {
    let internal_moving_mass_mark: Int = 0;
    if internal_moving_mass_mark != 0 {
        process_exit(1);
    }
    let a: MovingMassParams = moving_mass_params_default_glider();
    let b: MovingMassParams = moving_mass_params_default_glider();
    if (a).slider_mass_kg != (b).slider_mass_kg {
        println("FAIL moving_mass_params_default_glider");
        process_exit(1);
    }
    println("prod_internal_moving_mass: PASSED");
}
