// # Int mixed-arity weak attack
//
// Logline: Mix Int and Bool params so the int multi-arg rail fail-closes.
//
// Beats:
//   1. Keep FUZZ_DOMAIN int and target weak_mix 0 10.
//   2. Take Int, Bool, Int and return x.
//   3. Non-Int params must not silent-sample.

// Weak arity attack: multi-arg with non-Int param must fail-closed (not silent sample).
// FUZZ_DOMAIN: int
// FUZZ_TARGET: weak_mix 0 10
// FUZZ_ENSURES: weak_mix result >= 0
fn weak_mix(x: Int, y: Bool, z: Int) -> Int {
    return x;
}

pub fn main() {
}
