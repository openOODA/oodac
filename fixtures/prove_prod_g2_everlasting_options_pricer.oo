// # Product prove std/econ/defi_derivatives/everlasting_options_pricer.oo
//
// Logline: Fail-closed emit+gcc+run asserts ev_norm_pdf is deterministic.
//
// Setup: Isolated emit-c. Imports std/econ/defi_derivatives/everlasting_options_pricer.oo.
//
// Beats:
//   1. Call ev_norm_pdf twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_everlasting_options_pricer: PASSED only if they match.

import "std/science/econ/defi_derivatives/everlasting_options_pricer.oo";

pub fn main() {
    let everlasting_options_pricer_mark: Int = 0;
    if everlasting_options_pricer_mark != 0 {
        process_exit(1);
    }
    let a: Float = ev_norm_pdf(0.0);
    let b: Float = ev_norm_pdf(0.0);
    if a != b {
        println("FAIL ev_norm_pdf");
        process_exit(1);
    }
    println("prod_g2_everlasting_options_pricer: PASSED");
}
