// # Auto-generated signature-aware smoke stub for std/ui/media/audio_mixer.oo.
//
// Logline: Mixes multiple PCM audio tracks with individual gain scaling and anti-overflow saturation clipping.
// Setup: Pure audio DSP engine providing track summation and int16 dynamic range clamping.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_MEDIA_AUDIO_MIXER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/media/audio_mixer.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = audio_clamp_pcm16(0);
    let r2: Int = audio_clamp_pcm16(0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_MEDIA_AUDIO_MIXER_OO_STUB_OK");
}
