// # Product prove std/phys/quantum/density_matrix.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/phys/quantum/density_matrix.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_density_matrix: PASSED (reference).

import "std/science/phys/quantum/density_matrix.oo";

pub fn main() {
    let density_matrix_mark: Int = 0;
    if density_matrix_mark != 0 {
        process_exit(1);
    }
    println("prod_density_matrix: PASSED (reference)");
}
