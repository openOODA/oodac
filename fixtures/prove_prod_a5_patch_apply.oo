// # Product prove std/ui/web_ecosystem/patch_apply.oo
//
// Logline: Fail-closed emit+gcc+run asserts patch_apply_op is deterministic.
//
// Setup: Isolated emit-c. Imports std/ui/web_ecosystem/patch_apply.oo.
//
// Beats:
//   1. Call patch_apply_op twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_patch_apply: PASSED only if they match.

import "std/app/ui/web_ecosystem/patch_apply.oo";

pub fn main() {
    let a: Int = patch_apply_op(0, 0, 0);
    let b: Int = patch_apply_op(0, 0, 0);
    if a != b {
        println("FAIL patch_apply_op");
        process_exit(1);
    }
    println("prod_patch_apply: PASSED");
}
