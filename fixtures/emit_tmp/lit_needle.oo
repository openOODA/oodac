// # Literal oo_slist_get needle
//
// Logline: Bind a string literal that contains oo_slist_get( and print it.
//
// Setup: Backend-C emit fixture. The literal must not be treated as an emit needle.
//
// Beats:
//   1. Bind s to the literal oo_slist_get(.
//   2. Print s.
//   3. Emit must keep the text inside the string.

pub fn main() {
    let s = "oo_slist_get(";
    println(s);
}
