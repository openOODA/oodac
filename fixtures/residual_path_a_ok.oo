// # Keep a plain program checking OK
//
// Logline: A residual-suite path-A program still typechecks and prints zero.
//
// Beats:
//   1. Publish a main with no residual names.
//   2. Print zero as the only effect.
//   3. Prove the residual floor still accepts a clean unit.

// residual path A: plain program still checks OK
pub fn main() {
    println(0);
}
