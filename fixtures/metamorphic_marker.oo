// # Name the metamorphic residual
//
// Logline: This marker names the metamorphic residual surface without enforcing it.
//
// Beats:
//   1. Keep the residual marker line for honesty smokes.
//   2. Point at bootstrap/METAMORPHIC.oot as the surface doc.
//   3. Print zero so a named-only fixture still parses.

// Residual marker fixture — see bootstrap/METAMORPHIC.oot
// METAMORPHIC: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
