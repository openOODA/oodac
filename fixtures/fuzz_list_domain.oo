// # List-domain fuzz pass
//
// Logline: Identity-map a List[Int] so the pure list fuzz rail can pass.
//
// Beats:
//   1. Keep FUZZ_DOMAIN list and target id_list 0 4.
//   2. Ensure result == x and return x.
//   3. Leave main empty; the fuzzer drives the target.

// Pure List-domain fuzz pass (no Python).
// FUZZ_DOMAIN: list
// FUZZ_TARGET: id_list 0 4
// FUZZ_ENSURES: id_list result == x
fn id_list(x: List[Int]) -> List[Int] {
    return x;
}
pub fn main() {
}
