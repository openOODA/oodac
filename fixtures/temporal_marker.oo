// # Mark temporal residual
//
// Logline: Print 0 as a named TEMPORAL residual marker with no enforcement.
//
// Setup: Test fixture. Keep the TEMPORAL tag and all code exact.
//
// Beats:
//   1. Carry the TEMPORAL residual tag.
//   2. Name the surface only.
//   3. Print 0 from main.
//
// Residual marker fixture — see bootstrap/TEMPORAL_MEM.oot
// TEMPORAL: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
