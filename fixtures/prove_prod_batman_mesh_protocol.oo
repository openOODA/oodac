// # Product prove std/ai/graph_networks/batman_mesh_protocol.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/ai/graph_networks/batman_mesh_protocol.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_batman_mesh_protocol: PASSED (reference).

import "std/net/protocol/routing/batman_mesh_protocol.oo";

pub fn main() {
    let batman_mesh_protocol_mark: Int = 0;
    if batman_mesh_protocol_mark != 0 {
        process_exit(1);
    }
    println("prod_batman_mesh_protocol: PASSED (reference)");
}
