// # Int-domain fuzz pass
//
// Logline: Identity-map a non-negative Int so the pure int fuzz rail can pass.
//
// Beats:
//   1. Keep FUZZ_DOMAIN int and target abs_nonneg 0 100.
//   2. Require x >= 0 and ensure result >= 0.
//   3. Return x unchanged.

// Pure Int-domain fuzz pass fixture (no Python path).
// FUZZ_DOMAIN: int
// FUZZ_TARGET: abs_nonneg 0 100
// FUZZ_REQUIRES: abs_nonneg x >= 0
// FUZZ_ENSURES: abs_nonneg result >= 0
pub fn abs_nonneg(x: Int) -> Int
    requires x >= 0
    ensures result >= 0
{
    return x;
}

pub fn main() {
}
