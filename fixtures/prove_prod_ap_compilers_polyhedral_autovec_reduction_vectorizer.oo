// # Product prove std/compile/polyhedral/autovec_reduction_vectorizer.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/compile/polyhedral/autovec_reduction_vectorizer.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/compile/polyhedral/autovec_reduction_vectorizer.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_autovec_reduction_vectorizer: PASSED only if they match.

import "std/compile/polyhedral/autovec_reduction_vectorizer.oo";

pub fn main() {
    let autovec_reduction_vectorizer_a = compute_horizontal_reduction_steps(0);
    let autovec_reduction_vectorizer_b = compute_horizontal_reduction_steps(0);
    if autovec_reduction_vectorizer_a != autovec_reduction_vectorizer_b {
        println("FAIL autovec_reduction_vectorizer");
        process_exit(1);
    }
    println("prod_ap_autovec_reduction_vectorizer: PASSED");
}
