// # Product prove std/phys/astro_advanced/halo_orbit_cr3bp.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/astro_advanced/halo_orbit_cr3bp.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/astro_advanced/halo_orbit_cr3bp.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_halo_orbit_cr3bp: PASSED only if they match.

import "std/science/phys/relativistic/halo_orbit_cr3bp.oo";

pub fn main() {
    let halo_orbit_cr3bp_a = halo_libration();
    let halo_orbit_cr3bp_b = halo_libration();
    if halo_orbit_cr3bp_a != halo_orbit_cr3bp_b {
        println("FAIL halo_orbit_cr3bp");
        process_exit(1);
    }
    println("prod_ap_halo_orbit_cr3bp: PASSED");
}
