// # Product sync rcu list prove
//
// Logline: Fail-closed emit+gcc+run of shipped std/sync/rcu_list.oo.
//
// Setup: Product std/sync/rcu_list.oo. Isolated emit-c+gcc+run.
//
// Beats:
//   1. New len 0.
//   9. Exit 1 before PASSED on a miss.

import "std/net/sync/rcu_list.oo";

pub fn main() {
    let l: RcuList = rcu_list_new();
    let n: Int = rcu_list_len(l);
    if n != 0 {
        println("FAIL rculen 0");
        process_exit(1);
    }
    println("prod_rculen: PASSED");
}
