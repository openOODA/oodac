// # Auto-generated signature-aware smoke stub for std/ui/media_dsp/h264_sps_pps_decoder.oo.
//
// Logline: Decodes H.264 Sequence/Picture Parameter Sets, Exponential-Golomb bitstream codes, and frame resolutions.
// Setup: Implements ITU-T H.264 Section 7.3.2 syntax table parsing for macroblock video geometry extraction.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_MEDIA_DSP_H264_SPS_PPS_DECODER_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/app/ui/media_dsp/h264_sps_pps_decoder.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = h264_decode_ue_golomb(0, 0);
    let r2: Int = h264_decode_ue_golomb(0, 0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_MEDIA_DSP_H264_SPS_PPS_DECODER_OO_STUB_OK");
}
