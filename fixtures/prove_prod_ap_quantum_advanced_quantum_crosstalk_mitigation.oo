// # Product prove std/phys/quantum_advanced/quantum_crosstalk_mitigation.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/phys/quantum_advanced/quantum_crosstalk_mitigation.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/quantum_advanced/quantum_crosstalk_mitigation.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_quantum_crosstalk_mitigation: PASSED only if they match.

import "std/science/phys/quantum/quantum_crosstalk_mitigation.oo";

pub fn main() {
    let quantum_crosstalk_mitigation_a = crosstalk_zz_phase_accumulation(0.0, 0.0);
    let quantum_crosstalk_mitigation_b = crosstalk_zz_phase_accumulation(0.0, 0.0);
    if quantum_crosstalk_mitigation_a != quantum_crosstalk_mitigation_b {
        println("FAIL quantum_crosstalk_mitigation");
        process_exit(1);
    }
    println("prod_ap_quantum_crosstalk_mitigation: PASSED");
}
