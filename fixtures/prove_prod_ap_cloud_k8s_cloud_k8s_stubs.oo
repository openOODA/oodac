// # Product prove std/data/k8s/cloud_k8s_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/data/k8s/cloud_k8s_stubs.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/data/k8s/cloud_k8s_stubs.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_cloud_k8s_stubs: PASSED only if they match.

import "std/app/data/k8s/cloud_k8s_stubs.oo";

pub fn main() {
    let cloud_k8s_stubs_a = cloud_k8s_fail_closed("x", 0);
    let cloud_k8s_stubs_b = cloud_k8s_fail_closed("x", 0);
    if cloud_k8s_stubs_a != cloud_k8s_stubs_b {
        println("FAIL cloud_k8s_stubs");
        process_exit(1);
    }
    println("prod_ap_cloud_k8s_stubs: PASSED");
}
