// # Bool-domain fuzz fail
//
// Logline: Return x while ensuring result == !x so the bool fuzz rail fails.
//
// Beats:
//   1. Keep FUZZ_DOMAIN bool and target always_bad.
//   2. Ensure result == !x but return x.
//   3. Leave main empty; the fuzzer drives the target.

// Pure Bool-domain fuzz fail rail: ensures result == !x but body returns x.
// FUZZ_DOMAIN: bool
// FUZZ_TARGET: always_bad
// FUZZ_ENSURES: always_bad result == !x
fn always_bad(x: Bool) -> Bool {
    return x;
}
pub fn main() {
}
