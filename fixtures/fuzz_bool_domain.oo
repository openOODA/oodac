// # Bool-domain fuzz pass
//
// Logline: Identity-map a Bool so the pure bool fuzz rail can pass.
//
// Beats:
//   1. Keep FUZZ_DOMAIN bool and target id_bool.
//   2. Ensure result == x and return x.
//   3. Leave main empty; the fuzzer drives the target.

// Pure Bool-domain fuzz pass fixture (no Python path).
// FUZZ_DOMAIN: bool
// FUZZ_TARGET: id_bool
// FUZZ_ENSURES: id_bool result == x
fn id_bool(x: Bool) -> Bool {
    return x;
}
pub fn main() {
}
