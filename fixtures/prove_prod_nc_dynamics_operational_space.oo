// # Product prove std/robot/ros2/dynamics_operational_space.oo
//
// Logline: Fail-closed emit+gcc+run asserts dynamics_operational_space_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/robot/ros2/dynamics_operational_space.oo.
//
// Beats:
//   1. Call dynamics_operational_space_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_dynamics_operational_space: PASSED only if it matches.

import "std/app/robot/ros2/dynamics_operational_space.oo";

pub fn main() {
    let a: Int = dynamics_operational_space_probe();
    if a != 0 {
        println("FAIL dynamics_operational_space_probe");
        process_exit(1);
    }
    println("prod_nc_dynamics_operational_space: PASSED");
}
