// # Bool arity-3 weak attack
//
// Logline: Offer a three-Bool target so the bool multi-arg rail fail-closes.
//
// Beats:
//   1. Keep FUZZ_DOMAIN bool and target three_bool.
//   2. Take three Bools and return x.
//   3. Path A only samples arity-2, so this must fail closed.

// Weak arity attack: Bool multi-arg arity≥3 must fail-closed (M56 path only arity-2).
// FUZZ_DOMAIN: bool
// FUZZ_TARGET: three_bool
// FUZZ_ENSURES: three_bool result == x
fn three_bool(x: Bool, y: Bool, z: Bool) -> Bool {
    return x;
}

pub fn main() {
}
