// # Name the QA residual
//
// Logline: This marker names the QA-matrix residual surface without enforcing it.
//
// Beats:
//   1. Keep the residual marker line for honesty smokes.
//   2. Point at bootstrap/QA_MATRIX.oot as the surface doc.
//   3. Print zero so a named-only fixture still parses.

// Residual marker — see bootstrap/QA_MATRIX.oot
// QA: residual
pub fn main() { println(0); }
