// # Product prove std/robot/predictive/industrial_predictive_stubs.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/robot/predictive/industrial_predictive_stubs.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/predictive/industrial_predictive_stubs.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_industrial_predictive_stubs: PASSED only if they match.

import "std/app/robot/predictive/industrial_predictive_stubs.oo";

pub fn main() {
    let industrial_predictive_stubs_a = predictive_stub_vibration_adc_dma_status(0);
    let industrial_predictive_stubs_b = predictive_stub_vibration_adc_dma_status(0);
    if industrial_predictive_stubs_a != industrial_predictive_stubs_b {
        println("FAIL industrial_predictive_stubs");
        process_exit(1);
    }
    println("prod_ap_industrial_predictive_stubs: PASSED");
}
