// # Product prove std/math/graph_topology/k_core_decomposition.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/math/graph_topology/k_core_decomposition.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_k_core_decomposition: PASSED (reference).

import "std/core/math/graph_topology/k_core_decomposition.oo";

pub fn main() {
    let k_core_decomposition_mark: Int = 0;
    if k_core_decomposition_mark != 0 {
        process_exit(1);
    }
    println("prod_k_core_decomposition: PASSED (reference)");
}
