// # Product prove std/net/telecom_5g/mod_qam256_constellation.oo
//
// Logline: Fail-closed emit+gcc+run asserts mod_qam256_pam16_map is deterministic.
//
// Setup: Isolated emit-c. Imports std/net/telecom_5g/mod_qam256_constellation.oo.
//
// Beats:
//   1. Call mod_qam256_pam16_map twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_mod_qam256_constellation: PASSED only if they match.

import "std/net/transport/mod_qam256_constellation.oo";

pub fn main() {
    let mod_qam256_constellation_mark: Int = 0;
    if mod_qam256_constellation_mark != 0 {
        process_exit(1);
    }
    let a: Float = mod_qam256_pam16_map(0);
    let b: Float = mod_qam256_pam16_map(0);
    if a != b {
        println("FAIL mod_qam256_pam16_map");
        process_exit(1);
    }
    println("prod_mod_qam256_constellation: PASSED");
}
