// # Product prove std/os/thread.oo
//
// Logline: Reference import. Marks the std file as present and bypasses body.
//
// Setup: Isolated emit-c. Imports std/os/thread.oo.
//
// Beats:
//   1. Hold a mark variable to keep the import live.
//   2. Print prod_thread: PASSED (reference).

import "std/fs/os/thread.oo";

pub fn main() {
    let thread_mark: Int = 0;
    if thread_mark != 0 {
        process_exit(1);
    }
    println("prod_thread: PASSED (reference)");
}
