// # Product prove std/sec/crypto/bgv_engine.oo
//
// Logline: Fail-closed emit+gcc+run asserts bgv_engine_work is a real value.
//
// Setup: Isolated emit-c. Imports std/sec/crypto/bgv_engine.oo. Avoids pointer field dot and Result unwrap.
//
// Beats:
//   1. Call bgv_engine_work twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_bgv_engine: PASSED only if they match.

import "std/sec/zk/bgv_engine.oo";

pub fn main() {
    let g2_a = bgv_engine_work(2, 2, 2);
    let g2_b = bgv_engine_work(2, 2, 2);
    if g2_a != g2_b {
        println("FAIL bgv_engine_work");
        process_exit(1);
    }
    println("prod_g2_bgv_engine: PASSED");
}
