// # Product prove std/robot/auv/auv_line_of_sight_guidance.oo
//
// Logline: Fail-closed emit+gcc+run asserts los_guidance_config_default is deterministic.
//
// Setup: Isolated emit-c. Imports std/robot/auv/auv_line_of_sight_guidance.oo.
//
// Beats:
//   1. Call los_guidance_config_default twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_los_guidance_config_default: PASSED only if they match.

import "std/app/robot/auv/auv_line_of_sight_guidance.oo";

pub fn main() {
    let auv_line_of_sight_guidance_mark: Int = 0;
    if auv_line_of_sight_guidance_mark != 0 { process_exit(1); }
    let a: LosGuidanceConfig = los_guidance_config_default();
    let b: LosGuidanceConfig = los_guidance_config_default();
    if (a).lookahead_distance_m != (b).lookahead_distance_m {
        println("FAIL los_guidance_config_default");
        process_exit(1);
    }
    println("prod_los_guidance_config_default: PASSED");
}
