// # Product prove std/data/gis_topo/geoid_undulation_egm96.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/data/gis_topo/geoid_undulation_egm96.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/data/gis_topo/geoid_undulation_egm96.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_geoid_undulation_egm96: PASSED only if they match.

import "std/app/data/gis_topo/geoid_undulation_egm96.oo";

pub fn main() {
    let geoid_undulation_egm96_a = egm96_geoid_undulation(0.0, 0.0);
    let geoid_undulation_egm96_b = egm96_geoid_undulation(0.0, 0.0);
    if geoid_undulation_egm96_a != geoid_undulation_egm96_b {
        println("FAIL geoid_undulation_egm96");
        process_exit(1);
    }
    println("prod_ap_geoid_undulation_egm96: PASSED");
}
