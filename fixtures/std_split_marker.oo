// # Mark std-split residual
//
// Logline: Print 0 as a named STD_SPLIT residual marker with no enforcement.
//
// Setup: Test fixture. Keep the STD_SPLIT tag and all code exact.
//
// Beats:
//   1. Carry the STD_SPLIT residual tag.
//   2. Keep main as a marker body.
//   3. Print 0.
//
// Residual marker — see bootstrap/STD_SPLIT.oot
// STD_SPLIT: residual
pub fn main() { println(0); }
