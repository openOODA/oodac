// # Byte-string residual marker
//
// Logline: Name the BYTE_STR residual without shipping &str borrow.
//
// Beats:
//   1. Keep the BYTE_STR residual comment.
//   2. Note String stays value-copy.
//   3. Print 0 from main.

// Residual marker fixture — see bootstrap/BYTE_STR.oot
// BYTE_STR: residual
// Native &str borrow + Byte arrays not product. String value-copy remains.
pub fn main() {
    println(0);
}
