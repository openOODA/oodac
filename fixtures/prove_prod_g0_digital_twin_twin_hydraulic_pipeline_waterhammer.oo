// # Product prove std/robot/digital_twin/twin_hydraulic_pipeline_waterhammer.oo
//
// Logline: Fail-closed emit+gcc+run asserts waterhammer_joukowsky_surge is a real value.
//
// Setup: Isolated emit-c. Imports std/robot/digital_twin/twin_hydraulic_pipeline_waterhammer.oo. Avoids pointer field dot and Result unwrap.
//
// Beats:
//   1. Call waterhammer_joukowsky_surge twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g0_twin_hydraulic_pipeline_waterhammer: PASSED only if they match.

import "std/app/robot/digital_twin/twin_hydraulic_pipeline_waterhammer.oo";

pub fn main() {
    let g0_a = waterhammer_joukowsky_surge(1.0, 1.0, 1.0);
    let g0_b = waterhammer_joukowsky_surge(1.0, 1.0, 1.0);
    if g0_a != g0_b {
        println("FAIL waterhammer_joukowsky_surge");
        process_exit(1);
    }
    println("prod_g0_twin_hydraulic_pipeline_waterhammer: PASSED");
}
