// # Auto-generated signature-aware smoke stub for std/text/diff_match.oo.
//
// Logline: Myers difference algorithm, line-by-line unified diff formatting, LCS calculation, and text similarity scoring.
// Setup: Pure comparison engine computing additions, deletions, modifications, and diff statistics between text documents.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_TEXT_DIFF_MATCH_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/core/text/diff_match.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = diff_lcs_length("x", "x");
    let r2: Int = diff_lcs_length("x", "x");
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_TEXT_DIFF_MATCH_OO_STUB_OK");
}
