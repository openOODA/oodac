// # Product prove std/phys/quantum_topological/topological_quantum_memory.oo
//
// Logline: Fail-closed emit+gcc+run asserts int_to_float is deterministic.
//
// Setup: Isolated emit-c. Imports std/phys/quantum_topological/topological_quantum_memory.oo.
//
// Beats:
//   1. Call int_to_float twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_topological_quantum_memory: PASSED only if they match.

import "std/science/phys/quantum/topological_quantum_memory.oo";

pub fn main() {
    let topological_quantum_memory_mark: Int = 0;
    if topological_quantum_memory_mark != 0 {
        process_exit(1);
    }
    let a: Float = int_to_float(0);
    let b: Float = int_to_float(0);
    if a != b {
        println("FAIL int_to_float");
        process_exit(1);
    }
    println("prod_g2_topological_quantum_memory: PASSED");
}
