// # Concat two strings
//
// Logline: Add two owned strings and print the concatenated result.
//
// Setup: Test fixture. Keep all code exact.
//
// Beats:
//   1. Bind hi and yo.
//   2. Concat them with +.
//   3. Print the result.
//
// String + String: interp + CHS C + WASM bump-heap concat (no host strcat).
pub fn main() {
    let a = "hi";
    let b = "yo";
    let c = a + b;
    println(c);
}
