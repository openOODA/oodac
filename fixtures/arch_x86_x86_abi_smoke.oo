// # Auto-generated signature-aware smoke stub for std/hw/silicon/x86_abi.oo.
//
// Logline: Single-source-of-truth register set, calling convention, and stack alignment rules for the x86-64 native backend.
// Setup: Pure constants and helpers. Imports x86_regs for the hardware register encoding constants.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_ARCH_X86_X86_ABI_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/hw/silicon/x86_abi.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = abi_arg_reg(0);
    let r2: Int = abi_arg_reg(0);
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_ARCH_X86_X86_ABI_OO_STUB_OK");
}
