// # Product prove std/net/telecom_5g/ofdm_subcarrier_mapper.oo
//
// Logline: Fail-closed emit+gcc+run asserts ofdm_mapper_int_to_float is deterministic.
//
// Setup: Isolated emit-c. Imports std/net/telecom_5g/ofdm_subcarrier_mapper.oo.
//
// Beats:
//   1. Call ofdm_mapper_int_to_float twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_ofdm_subcarrier_mapper: PASSED only if they match.

import "std/net/transport/ofdm_subcarrier_mapper.oo";

pub fn main() {
    let ofdm_subcarrier_mapper_mark: Int = 0;
    if ofdm_subcarrier_mapper_mark != 0 {
        process_exit(1);
    }
    let a: Float = ofdm_mapper_int_to_float(0);
    let b: Float = ofdm_mapper_int_to_float(0);
    if a != b {
        println("FAIL ofdm_mapper_int_to_float");
        process_exit(1);
    }
    println("prod_g2_ofdm_subcarrier_mapper: PASSED");
}
