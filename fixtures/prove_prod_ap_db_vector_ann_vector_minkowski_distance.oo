// # Product prove std/data/vector_ann/vector_minkowski_distance.oo
//
// Logline: Fail-closed emit+gcc+run asserts std/data/vector_ann/vector_minkowski_distance.oo is deterministic.
//
// Setup: Isolated emit-c. Imports std/data/vector_ann/vector_minkowski_distance.oo.
//
// Beats:
//   1. Call twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_ap_vector_minkowski_distance: PASSED only if they match.

import "std/app/data/vector_ann/vector_minkowski_distance.oo";

pub fn main() {
    let vector_minkowski_distance_a = int_pow(0, 0);
    let vector_minkowski_distance_b = int_pow(0, 0);
    if vector_minkowski_distance_a != vector_minkowski_distance_b {
        println("FAIL vector_minkowski_distance");
        process_exit(1);
    }
    println("prod_ap_vector_minkowski_distance: PASSED");
}
