// # Auto-generated signature-aware smoke stub for std/encoding/binary_formats/macho_inspect.oo.
//
// Logline: Parses a Mach-O 64-bit header and counts LC_SEGMENT_64, LC_MAIN, LC_SYMTAB, and LC_DYLD_INFO load commands.
// Setup: Pure. Little-endian. Magic 4277009103 is MH_MAGIC_64.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_int.
//   4. Print STD_BINARY_FORMATS_MACHO_INSPECT_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/core/encoding/binary_formats/macho_inspect.oo";

fn expect_eq_int(label: String, got: Int, want: Int) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got.to_string() + " want=" + want.to_string());
    return false;
}

pub fn main() {
    let r: Int = macho_inspect_magic();
    let r2: Int = macho_inspect_magic();
    if !expect_eq_int("call1 ok", r, r) { return; }
    if !expect_eq_int("call2 ok", r2, r2) { return; }
    println("STD_BINARY_FORMATS_MACHO_INSPECT_OO_STUB_OK");
}
