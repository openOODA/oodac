// # Product prove std/data/database/vector_index_hnsw.oo
//
// Logline: Fail-closed emit+gcc+run asserts vector_index_hnsw_probe returns 0.
//
// Setup: Isolated emit-c. Imports std/data/database/vector_index_hnsw.oo.
//
// Beats:
//   1. Call vector_index_hnsw_probe.
//   2. Exit 1 if the result is not 0.
//   3. Print prod_nc_vector_index_hnsw: PASSED only if it matches.

import "std/app/data/database/vector_index_hnsw.oo";

pub fn main() {
    let a: Int = vector_index_hnsw_probe();
    if a != 0 {
        println("FAIL vector_index_hnsw_probe");
        process_exit(1);
    }
    println("prod_nc_vector_index_hnsw: PASSED");
}
