// # Name the package-ecosystem residual
//
// Logline: This marker names the package-ecosystem residual surface without enforcing it.
//
// Beats:
//   1. Keep the residual marker line for honesty smokes.
//   2. Point at bootstrap/PKG_ECOSYSTEM.oot as the surface doc.
//   3. Print zero so a named-only fixture still parses.

// Residual marker — see bootstrap/PKG_ECOSYSTEM.oot
// PKG_ECOSYSTEM: residual
pub fn main() { println(0); }
