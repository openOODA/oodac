// # Product prove std/crypto/zk_rollup/sparse_merkle_tree_history.oo
//
// Logline: Fail-closed emit+gcc+run asserts smt_checkpoint_new is deterministic.
//
// Setup: Isolated emit-c. Imports std/crypto/zk_rollup/sparse_merkle_tree_history.oo.
//
// Beats:
//   1. Call smt_checkpoint_new twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_sparse_merkle_tree_history: PASSED only if they match.

import "std/sec/zk/sparse_merkle_tree_history.oo";

pub fn main() {
    let sparse_merkle_tree_history_mark: Int = 0;
    if sparse_merkle_tree_history_mark != 0 {
        process_exit(1);
    }
    let a: SmtCheckpoint = smt_checkpoint_new(0, "x", 0, 0);
    let b: SmtCheckpoint = smt_checkpoint_new(0, "x", 0, 0);
    if (a).block_height != (b).block_height {
        println("FAIL smt_checkpoint_new");
        process_exit(1);
    }
    println("prod_sparse_merkle_tree_history: PASSED");
}
