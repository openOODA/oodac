// # Product prove std/ai/multimodal/vit_transformer_encoder.oo
//
// Logline: Fail-closed emit+gcc+run asserts vit_transformer_encoder_marker is deterministic.
//
// Setup: Isolated emit-c. Imports std/ai/multimodal/vit_transformer_encoder.oo.
//
// Beats:
//   1. Call vit_transformer_encoder_marker twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_g2_vit_transformer_encoder: PASSED only if they match.

import "std/app/ai/multimodal/vit_transformer_encoder.oo";

pub fn main() {
    let a: Int = vit_transformer_encoder_marker();
    let b: Int = vit_transformer_encoder_marker();
    if a != b {
        println("FAIL vit_transformer_encoder_marker");
        process_exit(1);
    }
    println("prod_g2_vit_transformer_encoder: PASSED");
}
