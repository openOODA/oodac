// # Product prove std/phys/space_propulsion/propellant_capillary_pmd.oo
//
// Logline: Fail-closed emit+gcc+run asserts capillary_pmd_spec_default_hydrazine value or determinism.
//
// Setup: Isolated emit-c. Imports std/phys/space_propulsion/propellant_capillary_pmd.oo.
//
// Beats:
//   1. Call capillary_pmd_spec_default_hydrazine twice with the same args.
//   2. Exit 1 if the comparable value misses.
//   3. Print prod_propellant_capillary_pmd: PASSED only if asserts hold.

import "std/science/phys/relativistic/propellant_capillary_pmd.oo";

pub fn main() {
    let propellant_capillary_pmd: CapillaryPmdSpec = capillary_pmd_spec_default_hydrazine();
    let propellant_capillary_pmd_b: CapillaryPmdSpec = capillary_pmd_spec_default_hydrazine();
    if (propellant_capillary_pmd).screen_pore_radius_um != 12.0 {
        println("FAIL capillary_pmd_spec_default_hydrazine screen_pore_radius_um");
        process_exit(1);
    }
    if (propellant_capillary_pmd).screen_pore_radius_um != (propellant_capillary_pmd_b).screen_pore_radius_um {
        println("FAIL capillary_pmd_spec_default_hydrazine det");
        process_exit(1);
    }
    println("prod_propellant_capillary_pmd: PASSED");
}
