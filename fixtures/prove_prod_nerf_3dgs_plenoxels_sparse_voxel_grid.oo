// # Product prove std/ai/nerf_3dgs/plenoxels_sparse_voxel_grid.oo
//
// Logline: Fail-closed emit+check asserts plenoxel_is_active is deterministic.
//
// Setup: Isolated emit-c. Imports std/ai/nerf_3dgs/plenoxels_sparse_voxel_grid.oo.
//
// Beats:
//   1. Call plenoxel_is_active twice with the same args.
//   2. Exit 1 if the two results differ.
//   3. Print prod_plenoxels_sparse_voxel_grid: PASSED only if they match.

import "std/app/ai/nerf_3dgs/plenoxels_sparse_voxel_grid.oo";

pub fn main() {
    let plenoxels_sparse_voxel_grid_mark: Int = 0;
    if plenoxels_sparse_voxel_grid_mark != 0 { process_exit(1); }
    let a: Int = plenoxel_is_active(0.5, 0.3);
    let b: Int = plenoxel_is_active(0.5, 0.3);
    if a != b { process_exit(1); }
    println("prod_plenoxels_sparse_voxel_grid: PASSED");
}
