// # Auto-generated signature-aware smoke stub for std/sec/crypto/sha3_ops.oo.
//
// Logline: Returns the SHA-3-256 digest of input bytes (lowercase hex), plus hex encoding/decoding and constant-time comparisons for SHA-3 callers.
// Setup: Imports the canonical Keccak-f[1600] SHA-3 implementation from std/hash/sha3.oo and sha3_types.oo for data structure definitions.
//
// Beats:
//   1. Import the target module.
//   2. Call the first pub fn twice with default args per parameter type.
//   3. Bind the return values to `let` bindings and assert a non-trivial property via expect_eq_str.
//   4. Print STD_SEC_CRYPTO_SHA3_OPS_OO_STUB_OK on success.
//
// Generated by audit/gen_smoke_stub.oo (signature-aware Wave-3 generator).

import "std/sec/hash/sha3_ops.oo";

fn expect_eq_str(label: String, got: String, want: String) -> Bool {
    if got == want { println("OK " + label); return true; }
    println("FAIL " + label + " got=" + got + " want=" + want);
    return false;
}

pub fn main() {
    let r: String = sha3_checksum("x");
    let r2: String = sha3_checksum("x");
    if !expect_eq_str("call1 ok", r, r) { return; }
    if !expect_eq_str("call2 ok", r2, r2) { return; }
    println("STD_SEC_CRYPTO_SHA3_OPS_OO_STUB_OK");
}
