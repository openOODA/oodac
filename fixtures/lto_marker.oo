// # LTO residual marker
//
// Logline: Name the LTO cross-language surface without enforcing it.
//
// Beats:
//   1. Keep the LTO residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/LTO_XLANG.oot
// LTO: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
