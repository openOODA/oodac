// # emit-x86 while loop that actually loops
//
// Logline: Loop twice, increment counter on stack, print "loop" each iteration.

pub fn main() {
    let mut x: Int = 0;
    while x < 2 {
        x = x + 1;
        println("loop");
    }
}
