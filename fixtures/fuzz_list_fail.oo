// # List-domain fuzz fail
//
// Logline: Return an empty list while ensuring result == x.
//
// Beats:
//   1. Keep FUZZ_DOMAIN list and target always_bad 1 3.
//   2. Ensure result == x.
//   3. Return list_new() so the rail fails.

// Pure List-domain fail rail: ensures result == x but returns empty.
// FUZZ_DOMAIN: list
// FUZZ_TARGET: always_bad 1 3
// FUZZ_ENSURES: always_bad result == x
fn always_bad(x: List[Int]) -> List[Int] {
    let mut y: List[Int] = list_new();
    return y;
}
pub fn main() {
}
