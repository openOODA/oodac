// # Hot-reload residual marker
//
// Logline: Name the hot-reload surface without enforcing it.
//
// Beats:
//   1. Keep the HOT_RELOAD residual comment.
//   2. Run a no-op main.
//   3. Print 0 as the marker payload.

// Residual marker fixture — see bootstrap/HOT_RELOAD.oot
// HOT_RELOAD: residual
// Named surface only. Not enforced.
pub fn main() {
    println(0);
}
