// # Probe victim: Result[String, String] callee bound as Result[Int, String]
//
// Logline: Hostile program. The cli_init_project shape at let. check and build must fail closed. No ELF.
//
// Setup: sres returns Result[String, String]. let r is Result[Int, String] from sres().
//
// Beats:
//   1. check exits nonzero with ERR.
//   2. build exits nonzero, prints ERR, and does not write an ELF.

fn sres() -> Result[String, String] {
    return Ok("x");
}

pub fn main() {
    let r: Result[Int, String] = sres();
    if r.is_ok() {
        println("no");
    }
}
