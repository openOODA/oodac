// # Probe victim: Result[String, String] callee assigned into Result[Int, String]
//
// Logline: Hostile program. The cli_init_project shape at mut assign. check and build must fail closed. No ELF.
//
// Setup: sres returns Result[String, String]. let mut r is Result[Int, String] then assigned sres().
//
// Beats:
//   1. check exits nonzero with ERR.
//   2. build exits nonzero, prints ERR, and does not write an ELF.

fn sres() -> Result[String, String] {
    return Ok("x");
}

pub fn main() {
    let mut r: Result[Int, String] = Ok(0);
    r = sres();
    if r.is_ok() {
        println("no");
    }
}
