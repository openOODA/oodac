// # Probe victim: Ok(String) bound as Result[Int, String]
//
// Logline: Hostile program. oodac check and build must fail closed. No ELF.
//
// Setup: annotated let of Result[Int, String] initialized with Ok("no").
//
// Beats:
//   1. check exits nonzero with ERR.
//   2. build exits nonzero, prints ERR, and does not write an ELF.

pub fn main() {
    let r: Result[Int, String] = Ok("no");
    if r.is_ok() {
        println("no");
    }
}
