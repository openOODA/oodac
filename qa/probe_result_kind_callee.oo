// # Probe victim: Result[String, String] callee returned from Result[Int, String] main
//
// Logline: Hostile program. The cli_init_project shape. check and build must fail closed. No ELF.
//
// Setup: sres returns Result[String, String]. main is Result[Int, String] and returns sres().
//
// Beats:
//   1. check exits nonzero with ERR.
//   2. build exits nonzero, prints ERR, and does not write an ELF.

fn sres() -> Result[String, String] {
    return Ok("x");
}

pub fn main() -> Result[Int, String] {
    return sres();
}
