// # Test OO GPU Free Authorized Pass
//
// Logline: Asserts valid compilation when oo_gpu_free receives valid &GpuCap token.
//
// Setup: Sealed GPU memory builtin oo_gpu_free should pass typecheck when authorized with &GpuCap.
//
// Beats:
//   1. Declare function taking gpu: &GpuCap.
//   2. Invoke oo_gpu_free with gpu token.
//   3. Assert static typecheck succeeds.

pub fn run_oo_gpu_free(gpu: &GpuCap) {
    let dev_ptr = 0;
    let res = oo_gpu_free(gpu, dev_ptr);
    match res {
        Ok(v) => {},
        Err(e) => {}
    }
}

pub fn main() {
}
