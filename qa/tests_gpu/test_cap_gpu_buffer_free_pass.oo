// # Test GPU Buffer Free Authorized Pass
//
// Logline: Asserts valid compilation when gpu_buffer_free receives valid &GpuCap token.
//
// Setup: Sealed GPU memory builtin gpu_buffer_free should pass typecheck when authorized with &GpuCap.
//
// Beats:
//   1. Declare function taking gpu: &GpuCap.
//   2. Invoke gpu_buffer_free with gpu token.
//   3. Assert static typecheck succeeds.

pub fn run_gpu_buffer_free(gpu: &GpuCap) {
    let dev_ptr = 0;
    let res = gpu_buffer_free(gpu, dev_ptr);
    match res {
        Ok(v) => {},
        Err(e) => {}
    }
}

pub fn main() {
}
