// # Test GPU Decorator Lexing
//
// Logline: Asserts valid lexing and compilation of @ symbol with GPU constructs.
//
// Setup: Lexer supports @ character for decorators.
//
// Beats:
//   1. Declare helper function.
//   2. Verify that @ does not crash compiler lexer.
//   3. Assert static typecheck succeeds.

pub fn run_decorator_test(gpu: &GpuCap) {
    let res = gpu_launch(gpu, "k_vec_add", 1, 64);
    match res {
        Ok(v) => {},
        Err(e) => {}
    }
}

pub fn main() {
}
