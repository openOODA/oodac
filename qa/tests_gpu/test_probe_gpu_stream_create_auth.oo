// # Test GPU Stream Create Authorized Pass
//
// Logline: Asserts valid compilation when gpu_stream_create receives &GpuCap token.
//
// Setup: Sealed GPU operations pass when &GpuCap token is provided.
//
// Beats:
//   1. Declare function taking gpu: &GpuCap.
//   2. Call gpu_stream_create with gpu token.
//   3. Assert static typecheck succeeds.

pub fn test_stream_create(gpu: &GpuCap) {
    let s = gpu_stream_create(gpu);
}

pub fn main() {
}
