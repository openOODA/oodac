// # Test GPU Stream Create Authorized Pass
//
// Logline: Asserts valid compilation when gpu_stream_create receives valid &GpuCap token.
//
// Setup: Sealed GPU builtin gpu_stream_create should pass typecheck when authorized with &GpuCap.
//
// Beats:
//   1. Declare function taking gpu: &GpuCap.
//   2. Invoke gpu_stream_create with gpu token.
//   3. Assert static typecheck succeeds.

pub fn run_stream_create(gpu: &GpuCap) {
    let res = gpu_stream_create(gpu);
    match res {
        Ok(s) => {},
        Err(e) => {}
    }
}

pub fn main() {
}
