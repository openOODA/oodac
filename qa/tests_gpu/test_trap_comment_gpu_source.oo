// # Test Comment String GPU Source Rejection Trap
//
// Logline: Asserts fail-closed rejection when source contains @gpu only in comments or string literals.
//
// Setup: Comments and string literals mention @gpu without authentic AST kernel definitions.
//
// Beats:
//   1. Include comments and string variables containing @gpu text.
//   2. Attempt HIP emission without real @gpu fn definitions.
//   3. Assert fail-closed rejection with ERR gpu_emit no @gpu kernel definitions found.

// @gpu pub fn fake_comment_kernel() {}

pub fn ordinary_fn() {
    let tag: String = "@gpu pub fn string_literal_kernel()";
    println(tag);
}

pub fn main() {
    ordinary_fn();
}
