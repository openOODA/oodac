// # Test GPU Sync Authorized Pass
//
// Logline: Asserts valid compilation when gpu_sync receives valid &GpuCap token.
//
// Setup: Sealed GPU builtin gpu_sync should pass typecheck when authorized with &GpuCap.
//
// Beats:
//   1. Declare function taking gpu: &GpuCap.
//   2. Invoke gpu_sync with gpu token.
//   3. Assert static typecheck succeeds.

pub fn run_gpu_sync(gpu: &GpuCap) {
    let res = gpu_sync(gpu);
    match res {
        Ok(v) => {},
        Err(e) => {}
    }
}

pub fn main() {
}
