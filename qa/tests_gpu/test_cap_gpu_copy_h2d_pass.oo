// # Test GPU Copy H2D Authorized Pass
//
// Logline: Asserts valid compilation when gpu_copy_h2d receives valid &GpuCap token.
//
// Setup: Sealed GPU builtin gpu_copy_h2d should pass typecheck when authorized with &GpuCap.
//
// Beats:
//   1. Declare function taking gpu: &GpuCap.
//   2. Invoke gpu_copy_h2d with gpu token.
//   3. Assert static typecheck succeeds.

pub fn run_gpu_copy_h2d(gpu: &GpuCap) {
    let dev_ptr = 0;
    let host_ptr = 0;
    let size = 1024;
    let res = gpu_copy_h2d(gpu, dev_ptr, host_ptr, size);
    match res {
        Ok(v) => {},
        Err(e) => {}
    }
}

pub fn main() {
}
