// # Probe victim: TimeCap must not satisfy arena_create
//
// Logline: Hostile program. oodac check and oodac build must both fail closed. No ELF.
//
// Setup: main takes &TimeCap. arena_create is sealed ArenaCap.
//
// Beats:
//   1. check exits nonzero with ERR capability.
//   2. build exits nonzero, prints ERR, and does not write an ELF.

pub fn main(t: &TimeCap) {
    let a: Result[String, String] = arena_create(t, 4096);
    println("no");
}
