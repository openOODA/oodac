// # Parse integer text for lexer coordinates
//
// Logline: Converts integer digit text into an Int for line and column offsets.
//
// Setup: Parses digit strings into Int values.
//
// Beats:
//   1. Checks sign and scans digits.
//   2. Computes integer value.
//   3. Returns signed result.

pub fn parse_int(s: String) -> Int {
    let n = chars_len(s);
    let mut v = 0;
    let mut i = 0;
    let mut sign = 1;
    if n > 0 {
        if char_at(s, 0) == "-" {
            sign = -1;
            i = 1;
        }
    }
    while i < n {
        let c = char_at(s, i);
        if c == "0" { v = v * 10 + 0; }
        else if c == "1" { v = v * 10 + 1; }
        else if c == "2" { v = v * 10 + 2; }
        else if c == "3" { v = v * 10 + 3; }
        else if c == "4" { v = v * 10 + 4; }
        else if c == "5" { v = v * 10 + 5; }
        else if c == "6" { v = v * 10 + 6; }
        else if c == "7" { v = v * 10 + 7; }
        else if c == "8" { v = v * 10 + 8; }
        else if c == "9" { v = v * 10 + 9; }
        i = i + 1;
    }
    return v * sign;
}
