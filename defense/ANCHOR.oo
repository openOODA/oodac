// # Defense & Metamorphic Compilation — Module Entry Door
//
// Logline: Re-exports metamorphic passes, opaque predicates, and instruction mutation interfaces.
//
// Setup: Imports internal defense siblings and provides clean public re-exports.
//
// Beats:
//   1. Import metamorphic transformation and mutation siblings.
//   2. Re-export public defense compilation pass APIs.
//   3. Maintain <= 256 lines limit.

import "lex/ANCHOR.oo";
import "defense_mutate.oo";
import "defense_pass.oo";

// ──▓▓ Public Defense API ──▓▓──

pub fn defense_pass_run(stream: List[String], seed: Int) -> List[String] {
    return meta_drive_pass_run(stream, seed);
}

pub fn defense_mutate_op(insn: String) -> String {
    return meta_insn_subst_op(insn);
}
